---
import { withBase } from '../lib/url';

interface Props {
  href: string;
  class?: string;
  [key: string]: unknown;
}

const { href, class: className, ...rest } = Astro.props;
const finalHref = withBase(href);
---

<a href={finalHref} class={className} {...rest}>
  <slot />
</a>
