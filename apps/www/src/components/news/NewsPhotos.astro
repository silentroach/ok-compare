---
import type { NewsPhoto } from '../../lib/news/schema';

interface Props {
  readonly photos: readonly NewsPhoto[];
  readonly title?: string;
  readonly heading?: 'h2' | 'h3' | 'h4';
  readonly framed?: boolean;
}

const { photos, title = 'Фото', heading = 'h2', framed = true } = Astro.props;

const Heading = heading;
---

{
  photos.length > 0 && (
    <section class:list={framed && ['ui-shell px-5 py-6 md:px-6 md:py-7']}>
      <div class="space-y-4">
        <Heading class="text-2xl font-semibold tracking-tight text-foreground">
          {title}
        </Heading>

        <div class="grid gap-4 md:grid-cols-2">
          {photos.map((photo) => (
            <figure class="space-y-2">
              <img
                src={photo.url}
                alt={photo.alt}
                loading="lazy"
                class="w-full border border-border bg-surface-muted object-cover"
              />
              {photo.caption && (
                <figcaption class="text-sm leading-6 text-muted-foreground">
                  {photo.caption}
                </figcaption>
              )}
            </figure>
          ))}
        </div>
      </div>
    </section>
  )
}
