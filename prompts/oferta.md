# Обновление оферт с сайта ОК Комфорт

Открой страницу https://ok-komfort.ru/objects/shelkovo-eco-club/ с помощью `agent-browser`. На ней опубликованы PDF-документы. Нас интересуют только публичные договоры.

Сначала составь список найденных договоров: название ссылки, исходный URL, slug части поселка и путь к KB-статье в `apps/www/src/data/kb/ok/offer`. Не обрабатывай приказы, регламенты и другие документы. Для каждого публичного договора запускай отдельного субагента. Обрабатывай договоры последовательно, не параллельно.

До окончания всей задачи веди журнал в `/private/tmp/opencode/oferta-progress.md`. Для каждого договора записывай исходный URL, slug, путь статьи, дату снимка, S3 key и статусы скачивания, загрузки, OCR, редактирования и проверки. При повторном запуске продолжай с первого незавершенного этапа, не выполняя успешно завершенные этапы заново. После полного успеха удали журнал.

## Оригинал и архив

Для каждого договора:

1. Скачай исходный PDF без каких-либо преобразований во временную папку. Именно этот файл считается оригиналом и загружается в архив. OCR-версию в S3 не загружай.
2. Проверь через `file` и `pdfinfo`, что скачался корректный PDF, а не HTML-страница или ответ с ошибкой. Рассчитай SHA-256 файла.
3. Если существующая статья уже ссылается на архивный PDF этого договора, скачай его и сравни SHA-256. При совпадении сохрани существующие дату снимка, S3 key и URL, не создавай новый объект и переходи к распознаванию. При различии продолжай как с новой редакцией.
4. Определи дату снимка в формате `YYYY-MM-DD`:
   - для новой статьи или новой редакции используй фактическую дату скачивания в `Europe/Moscow`;
   - при переносе существующей статьи можно использовать дату первого Git-коммита, который подтверждает эту редакцию; если связь с редакцией не доказуема, используй фактическую дату скачивания;
   - не используй filesystem creation date, S3 `Last-Modified` или автоматически дату внутри договора.
5. Сформируй ключ `kb/ok/offer/<snapshot-date>/<slug>.pdf`, например `kb/ok/offer/2026-07-07/shelkovo-park.pdf`.
6. Опубликуй оригинал по skill `public-media-publisher` и `ops/storage/public-media.md`. Для PDF используй MIME-тип `application/pdf`; не перезаписывай существующий объект с другим SHA-256.
7. Публичный URL объекта должен иметь вид `https://media.kpshelkovo.online/kb/ok/offer/<snapshot-date>/<slug>.pdf`. Не продолжай, пока через media-origin не проверены статус `200`, MIME-тип, размер и SHA-256.

## Распознавание и статья

После успешного архивирования оригинала:

1. Запусти OCR и сохрани результат в отдельный файл:

   ```bash
   ocrmypdf -l rus+eng --output-type pdfa --rotate-pages --deskew -O3 --jpeg-quality 85 \
     "<local-original.pdf>" "<local-ocr.pdf>"
   ```

2. Преобразуй OCR-версию в Markdown:

   ```bash
   docker run --rm -i adeuxy/markitdown:latest < "<local-ocr.pdf>" > "<parsed.md>"
   ```

3. Исправь в Markdown только явные опечатки и артефакты OCR. Ничего не придумывай и не дополняй от себя.
4. Создай или обнови статью `apps/www/src/data/kb/ok/offer/<slug>.md`.
5. Сразу после frontmatter добавь сноску с прямой ссылкой на архивный PDF:

   ```markdown
   > Документ распознан автоматически, перед использованием сверяйте текст с [оригиналом](https://media.kpshelkovo.online/kb/ok/offer/<snapshot-date>/<slug>.pdf).
   ```

6. После сноски добавь `[TOC]` отдельной строкой. При публикации новой редакции замени ссылку в сноске на новый архивный PDF.

## Индекс оферт

После обработки всех документов обнови индексный KB-документ `apps/www/src/data/kb/ok/index.md`: собери все оферты в одной группе и дай ссылки на статьи списком. Если для одной части поселка есть несколько оферт, сгруппируй их в одной строке.

## Правила редактирования

- Заголовок страницы: `Публичная оферта - [Название части поселка с нюансами из названия ссылки на исходной странице]`.
- Нельзя ничего придумывать или додумывать. Допустимы только небольшие исправления явных ошибок OCR.
- Если в оферте есть сноски со звездочками, замени звездочку в тексте содержимым сноски в скобках.
- Между приложениями к договору ставь `---`.
- Проверяй, что списки и параграфы сохранили структуру оригинала.
- Если приложение с QR-кодом после распознавания пустое, добавь редакторский текст о том, что QR-код нужно открыть в оригинальном PDF.
- Упоминания приложений превращай во внутренние ссылки на соответствующие разделы статьи.
- Упоминания законов и норм по возможности связывай с их официальными публикациями или надежными правовыми источниками. Не добавляй ссылку, если не уверен, что нашел нужный документ.
- Если номер статьи или нормы указан в скобках перед названием закона (например, «(ст. 438 ГК РФ)»), оформляй ссылку на конкретную статью, а не на весь кодекс. Добавь отдельный reference на страницу этой статьи на consultant.ru и используй его: `([ст. 438 ГК РФ][ст. 438 ГК РФ])`.
- Если в тексте перечислены номера статей без скобок (например, «статей 426, 437 Гражданского кодекса Российской Федерации»), делай ссылки именно на номера статей: `статей [426][ст. 426 ГК РФ], [437][ст. 437 ГК РФ] Гражданского кодекса Российской Федерации`. Не оборачивай в ссылку название закона, если рядом уже есть ссылки на конкретные статьи.
- Reference `[ГК РФ]` оставляй только когда речь идёт о кодексе в целом, а не о конкретной статье (например, «в соответствии с [Гражданским кодексом РФ][ГК РФ]»).
- Если в тексте указан конкретный нормативный акт регулятора (например, «Распоряжение Комитета по ценам и тарифам Московской области [№ 372-Р от 19.12.2025 года][Распоряжение 372-Р]»), делай ссылку на официальную публикацию, например на `publication.pravo.gov.ru`. Не добавляй ссылку, если не уверен, что нашёл нужный документ.
- Номера цветов RAL связывай с `https://ral.ru/ral<номер>`, например RAL 8017 — с `https://ral.ru/ral8017`.
- Исправляй очевидные ошибки в записи времени, например `9.00-18.00` на `9:00-18:00`.
- После вложенного или маркированного списка отделяй следующий юридический пункт HTML-комментарием `<!-- break list -->`, а сам пункт начинай с новой строки: `1.2.`, `3.6.`, `4.1.1.`. Иначе Prettier может вернуть пункт внутрь предыдущего списка и сломать HTML-разметку.
- Если приложение начинается со служебной строки и отдельного смыслового заголовка, объедини их в один заголовок.

Было:

```markdown
## Приложение № 5

к Публичному договору с обслуживающей компанией от 09.06.2026 года

### Технические Условия на организацию въездной группы
```

Стало:

```markdown
## Приложение № 5: Технические Условия на организацию въездной группы
```

## Финальная проверка

Перед завершением:

1. Проверь все публичные PDF-URL повторно.
2. Проверь, что каждая статья ссылается на PDF с тем же slug.
3. Проверь, что старые архивные объекты в S3 не перезаписаны.
4. Запусти релевантные проверки KB и сборку сайта.
5. Покажи итоговый список: название договора, исходный URL, S3 key, публичный URL и результат проверки.
