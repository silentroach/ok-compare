{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [".env*", "apps/*/.env*"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/site/**"]
    },
    "build:section": {
      "dependsOn": ["^build"],
      "outputs": ["dist/section/**"]
    },
    "build:legacy": {
      "dependsOn": ["^build"],
      "outputs": ["dist/legacy/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    }
  }
}
