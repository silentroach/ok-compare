# Scratchpad

## 2026-05-21

Planner continuation for `docs/migrations/raw-to-domain-data/`: existing docs showed Step 4 verified but runtime task `task-1779309212-46e0` was still `in_progress`. Closed Step 4 based on recorded tests/typecheck, advanced `progress.md` to Step 5, ensured only the Step 5 runtime task wave with key `code-assist:raw-to-domain-data:step-05:compare-public-ui-migration`, and prepared to emit `tasks.ready` for Builder.

Builder Step 5: added RED architecture guard for raw settlement tokens in compare route/island code, migrated `ExplorerSettlement` and compare Svelte/route reads to camelCase, updated domain table/source component props, and kept legacy `settlements.json` snake_case only in `full.ts`. Focused compare tests and typecheck passed before final handoff.

Critic Step 5 review: re-checked runtime task `task-1779345053-771f`, read ADR-013/ADR-011/context/task, inspected compare route/adapters/discovery/llms code, ran Svelte autofixer on changed compare components, ran `pnpm --filter @shelkovo/www test -- src/compare`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and validated built `settlements.json`/`explorer.json` shapes. Checks passed, but review rejects: `FullPayload`/`ExplorerPayload` still expose domain `Stats` and `ComparisonResult` directly and JSON routes assemble payload objects themselves. Step 05 acceptance requires public compare JSON through dedicated public DTO adapters, and ADR-013 says public DTO must not be the domain model directly. Builder should add explicit public payload DTO/adapters for stats/comparisons or a whole-payload adapter and update tests accordingly.

Builder Step 5 rejection fix: added a RED architecture guard for public compare JSON boundaries, then introduced explicit public stats/comparison DTO adapters in `apps/www/src/compare/lib/public-dto.ts` plus whole-payload adapters `toFullPayload` and `toExplorerPayload`. Updated both JSON routes to call those adapters instead of assembling payloads inline. Verification passed: focused adapter/architecture tests, full `src/compare` tests, `@shelkovo/www` typecheck, `@shelkovo/www` build, and a built JSON shape check for both compare data feeds.

Critic Step 5 re-review: re-checked runtime task `task-1779345053-771f`, ADR-013, ADR-011 and the Step 05 artifact. Inspected compare routes, public DTO adapters, discovery/llms text, route/island architecture guards and changed Svelte components. Svelte autofixer reported no errors; only non-blocking existing suggestions in `SettlementsExplorer.svelte` around effect-managed fetch state and a local `Map`. Verification passed: focused public DTO/architecture tests, compare component tests, full `pnpm --filter @shelkovo/www test -- src/compare`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built JSON harness asserting `settlements.json` keeps the intentional legacy full contract while `explorer.json` exposes camelCase slim DTO without raw settlement fields. No blocking findings; publish `review.passed`.

Finalizer Step 5: re-read `context.md`, `plan.md`, `progress.md`, Task 05, ADR-013, ADR-011, and runtime task `task-1779345053-771f`. Svelte autofixer found no errors in changed compare components; `SettlementsExplorer.svelte` still has the known non-blocking suggestions around effect-managed fetch state and local `Map`. Verification passed: `pnpm --filter @shelkovo/www test -- src/compare`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built JSON harness confirming `data/explorer.json` has camelCase slim DTO without raw settlement fields while legacy `data/settlements.json` keeps snake_case fields. Closed `task-1779345053-771f`. Plan still has incomplete Steps 6-13, so publish `queue.advance` rather than `LOOP_COMPLETE`.

Planner continuation after Step 5 finalization: ready queue was empty and all Step 5 runtime work was closed. Advanced `progress.md` to Step 6 - News raw/domain foundation, ensured one current-wave runtime task `task-1779346730-0c77` with key `code-assist:raw-to-domain-data:step-06:news-raw-domain-foundation`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/06-news-raw-domain-foundation.md`.

Builder Step 6: started runtime task `task-1779346730-0c77`, read ADR-013, migration context, news rules and task artifact. Added RED tests for the news raw/domain boundary and author mapper, then added `raw-schema.ts`, `types.ts`, and `mapper.ts`. `content.config.ts` now imports `RawNewsAuthorSchema` and `createRawNewsArticleSchema`; `schema.ts` keeps news constants/helpers and re-exports domain types; `NewsAuthor.short_name` is mapped to domain `shortName`. Verification passed: focused RED/GREEN tests, full `pnpm --filter @shelkovo/www test -- src/lib/news`, and `pnpm --filter @shelkovo/www typecheck`.

Critic Step 6 review: re-checked runtime task `task-1779346730-0c77`, ADR-013/ADR-011, migration context, task artifact, news local rules and Step 6 implementation. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`. Review rejects because `apps/www/src/components/news/NewsMeta.astro` still types `author` as `Pick<NewsAuthor, 'name' | 'short_name' | 'url'>`; after `NewsAuthor.shortName` migration this leaves a raw author field in the news UI contract and advertises a prop shape whose short author display is ignored by `formatNewsAuthor`. Builder should switch this component contract to `shortName` and add/extend a guard so raw `short_name` cannot remain in news app/component contracts outside raw schema/mapper/tests.

Builder Step 6 rejection fix: added a RED news architecture guard that scans news UI contracts for raw author `short_name`; it failed on `NewsMeta.astro` as expected. Migrated the `NewsMeta` author prop to `shortName`, confirmed no `short_name` tokens remain in news components/pages, and kept the fix limited to the rejected Step 6 issue. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`.

Critic Step 6 re-review: re-checked runtime task `task-1779346730-0c77`, ADR-013, ADR-011, migration context/task artifact, app/news rules, and the rejection fix. Inspected `RawNewsAuthorSchema`, `createRawNewsArticleSchema`, `NewsAuthor.shortName`, mapper/load integration, `NewsMeta.astro`, and the news architecture guard. Verified no `short_name` remains in news components/pages and only raw schema/mapper/tests keep the raw author field in `src/lib/news`. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and built news output grep for `short_name`/`shortName` plus a rendered article author check. No blocking findings; publish `review.passed`.

Finalizer Step 6: re-read migration context/plan/progress, Task 06, ADR-013, ADR-011, app/news rules, and runtime task `task-1779346730-0c77`. Inspected raw schema/type/mapper/content config/news UI boundaries and confirmed the review rejection was resolved by moving `NewsMeta.astro` to `author.shortName` plus a guard against raw `short_name` in news UI contracts. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, built `/news/data/articles.json` has 14 articles with no `short_name`, and the seeded rendered article shows the expected author label. Steps 7-13 remain incomplete, so close Step 6 runtime task and publish `queue.advance`.

Planner continuation after Step 6 finalization: ready queue was empty and Step 6 runtime work was closed. Advanced `progress.md` to Step 7 - News article domain migration, ensured current-wave runtime task `task-1779348032-8b0c` with key `code-assist:raw-to-domain-data:step-07:news-article-domain-migration`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/07-news-article-domain-migration.md`.

Builder Step 7: started runtime task `task-1779348032-8b0c`, read ADR-013/ADR-011, migration docs, app/news rules, and the Step 07 artifact. Added RED architecture/load tests for camelCase news article/event/dataset domain fields. Migrated article, list, event, archive, tag, home and dataset internals to camelCase, updated news UI/rendering/mentions/calendar/RSS/llms call sites, and introduced `src/lib/news/public-dto.ts` so legacy `articles.json` remains a public DTO adapter output rather than the domain model. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built JSON shape check confirmed the public feed still exposes legacy snake_case URL keys.

Critic Step 7 review: re-checked runtime task `task-1779348032-8b0c`, ADR-013, ADR-011, migration context/progress, app/news rules and the Step 07 artifact. Inspected news domain types, loader, public DTO adapter, discovery/llms/markdown/calendar/UI call sites and ran `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, plus a built `/news/data/articles.json` harness confirming 14 articles and legacy public `published_at`/`html_url`/`markdown_url` shape without camelCase domain key leaks. Review rejects: `apps/www/src/lib/sitemap.ts` and `apps/www/src/lib/sitemap-data.ts` still expose news article sitemap contracts with `news_articles`, `published_iso`, and `updated_iso` as internal TypeScript field names outside raw schema/mapper/public DTO/test fixtures, and the Step 07 architecture guard misses those files. Builder should migrate those news sitemap input/source contract names to camelCase or move them behind an explicit public/raw adapter boundary, then extend the guard to cover this regression surface.

Builder Step 7 rejection fix: added the rejected sitemap files to the news raw/domain architecture guard and included `news_articles` in the raw article token list; the focused guard failed on `sitemap.ts` as expected. Migrated the news sitemap source/input contract to `newsArticles`, `publishedIso`, and `updatedIso` in `sitemap.ts`, `sitemap-data.ts`, and `sitemap.test.ts`, deliberately leaving status/compare sitemap snake_case fields untouched for their own migration steps. Code-simplification pass found no smaller safe change beyond the direct rename. Verification passed: focused sitemap/news architecture tests, full `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`.

Critic Step 7 re-review: re-checked runtime task `task-1779348032-8b0c`, ADR-013, ADR-011, migration context/progress, app/news rules, and the Step 07 artifact. Inspected news domain types, loader boundary, public DTO adapter, discovery/llms/markdown/RSS surfaces, and the sitemap rejection fix. Confirmed `SitemapNewsArticleInput` and source data now use `newsArticles`, `publishedIso`, and `updatedIso`; the architecture guard covers `src/lib/sitemap.ts` and `src/lib/sitemap-data.ts` for raw news article tokens. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www test -- src/lib/news/index.architecture.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built `/news/data/articles.json` harness confirming 14 articles, legacy public `published_at`/`html_url`/`markdown_url`, absolute `events[].ics_url`, and no camelCase domain key leaks. No blocking findings; publish `review.passed`.

Finalizer Step 7: re-read migration context/plan/progress, Task 07, ADR-013, ADR-011, app/news rules, news content rules, and runtime task `task-1779348032-8b0c`. Confirmed the sitemap rejection fix is represented in the architecture guard and `sitemap.ts`/`sitemap-data.ts` now use `newsArticles`, `publishedIso`, and `updatedIso` for news internals. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www test -- src/lib/news/index.architecture.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built JSON harness confirming `/news/data/articles.json` has 14 articles, 4 events, legacy public `published_at`/`html_url`/`markdown_url`, absolute `events[].ics_url`, and no camelCase domain key leaks. Recorded fix memory `mem-1779349783-7a36` for a resolved inline Node quoting failure. Steps 8-13 remain incomplete, so close Step 7 runtime task and publish `queue.advance` rather than `LOOP_COMPLETE`.

Planner continuation after Step 7 finalization: ready queue was empty and Step 7 runtime work was closed. Advanced `progress.md` to Step 8 - Status raw/domain foundation, ensured current-wave runtime task `task-1779349886-8a97` with key `code-assist:raw-to-domain-data:step-08:status-raw-domain-foundation`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/08-status-raw-domain-foundation.md`.

Builder Step 8: started runtime task `task-1779349886-8a97`, read ADR-013/ADR-011, migration docs, app rules, and the Step 08 artifact. Added RED coverage for reusable status raw schema, explicit raw status value mappers, and camelCase status dataset fields. Implemented `RawStatusIncidentSchema`, handwritten readonly status domain types, `mapRawStatusIncident` and explicit service/kind/area mappers; migrated status loader/domain consumers to camelCase while preserving timeline DOM tooltip JSON and public discovery payload as DTO boundaries. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts` and `pnpm --filter @shelkovo/www typecheck`.

Critic Step 8 review: re-checked runtime task `task-1779349886-8a97`, ADR-013, ADR-011, migration context/progress, app rules and the Step 08 artifact. Inspected status raw schema, domain types, mapper, loader, status UI/markdown/discovery/timeline consumers, and sitemap surfaces. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www test -- src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www build`, and a built output harness confirming status JSON keeps the intentional public snake_case contract without camelCase domain key leaks. Review rejects: `apps/www/src/lib/sitemap.ts` and `apps/www/src/lib/sitemap-data.ts` still expose status sitemap source/input contracts as `status_incidents`, `started_iso`, `ended_iso`, and `has_page`, which are internal build-time status fields outside raw schema/mapper/public DTO boundaries. Builder should migrate the status sitemap contract to camelCase (`statusIncidents`, `startedIso`, `endedIso`, `hasPage`) or put it behind an explicit adapter, and extend a guard/test so this regression surface is covered.

Builder Step 8 rejection fix: added a RED status architecture guard in `schema.test.ts` scanning `sitemap.ts` and `sitemap-data.ts` for raw status sitemap tokens; it failed on `status_incidents`, `started_iso`, `ended_iso`, and `has_page` as expected. Migrated only status sitemap internals to `statusIncidents`, `startedIso`, `endedIso`, and `hasPage` in `sitemap.ts`, `sitemap-data.ts`, and `sitemap.test.ts`, leaving public status JSON and timeline tooltip DTO snake_case boundaries untouched. Code-simplification pass found no smaller safe change beyond the direct rename. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`. Recorded expected RED failure memory `mem-1779351070-06ea`.

Critic Step 8 re-review: re-checked runtime task `task-1779349886-8a97`, ADR-013, ADR-011, migration context/progress, app rules, and the Step 08 artifact. Inspected status raw schema, readonly domain types, mapper/load integration, status discovery/timeline DTO boundaries, and the sitemap rejection fix. Confirmed `SitemapStatusIncidentInput` and source data now use `statusIncidents`, `startedIso`, `endedIso`, and `hasPage`, with the architecture guard covering `src/lib/sitemap.ts` and `src/lib/sitemap-data.ts` for raw status sitemap tokens. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built status JSON/sitemap harness confirming legacy public `started_at`/`is_active` stays published while camelCase domain keys do not leak. No blocking findings; publish `review.passed`.

Finalizer Step 8: re-read migration context/plan/progress, Task 08, ADR-013, ADR-011, app rules, and runtime task `task-1779349886-8a97`. Confirmed the status sitemap rejection fix is represented in the architecture guard and `sitemap.ts`/`sitemap-data.ts` now use `statusIncidents`, `startedIso`, `endedIso`, and `hasPage` for status internals. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built status JSON/sitemap harness confirming `/status/data/status.json` has 18 incidents, legacy public `started_at`/`is_active`/`source_url`, no camelCase status domain key leaks, and sitemap status URLs without raw status sitemap tokens. Steps 9-13 remain incomplete, so close Step 8 runtime task and publish `queue.advance` rather than `LOOP_COMPLETE`.

Planner continuation after Step 8 finalization: ready queue was empty and Step 8 runtime work was closed. Advanced `progress.md` to Step 9 - Status UI and public adapters, ensured current-wave runtime task `task-1779351581-a498` with key `code-assist:raw-to-domain-data:step-09:status-public-ui-migration`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/09-status-public-ui-migration.md`.

Builder Step 9: started runtime task `task-1779351581-a498`, read ADR-013/ADR-011, migration context/plan/progress, app rules, and the Step 09 artifact. Added RED coverage for status UI/browser DTO raw field leakage and updated grouped timeline snapshot expectation to camelCase tooltip JSON; RED failed on `StatusServiceTimeline.astro` and `timeline.dom.ts` as expected. Implemented `src/lib/status/timeline-tooltip-dto.ts`, migrated timeline grouped tooltip serialization/parsing to camelCase (`isActive`, `startedIso`, `startedHasTime`, `endedIso`, `endedHasTime`, `duration.totalMinutes`), and moved the legacy status JSON payload adapter to explicit `src/lib/status/public-dto.ts` with `/status/data/status.json` routed through `buildStatusPublicPayload`. Code-simplification pass removed duplicate duration DTO creation. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built harness confirming status JSON preserves legacy snake_case public keys while status HTML no longer leaks raw tooltip DTO keys.

Critic Step 9 review: re-checked runtime task `task-1779351581-a498`, ADR-013, ADR-011, migration context/progress, app rules, and the Step 09 artifact. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`. Review rejects because `apps/www/src/components/status/StatusIncidentMeta.astro` still uses raw status UI fields `incident.source_url` and `incident.applies_to_all_areas`; this violates Step 09's status component acceptance criteria and drops rendered source links because domain `StatusIncident` now exposes `sourceUrl`/`appliesToAllAreas`. The Step 09 guard only scans `StatusServiceTimeline.astro` and `timeline.dom.ts`, so it misses this status UI regression surface. Builder should migrate `StatusIncidentMeta.astro` to domain fields and extend the status UI architecture guard to include this component and raw status tokens such as `source_url`/`applies_to_all_areas`.

Builder Step 9 rejection fix: added RED coverage to the status UI/browser DTO guard for `StatusIncidentMeta.astro` and raw tokens `source_url`/`applies_to_all_areas`; the focused guard failed on that component as expected. Migrated `StatusIncidentMeta.astro` to domain `sourceUrl` and `appliesToAllAreas`, with no broader status/public DTO changes. Code-simplification pass found no smaller safe change beyond this direct rename and guard extension. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, plus built status HTML grep for the rejected raw tokens.

Critic Step 9 re-review: re-checked runtime task `task-1779351581-a498`, ADR-013, ADR-011, migration context/progress, app rules, and the Step 09 artifact. Inspected the rejection fix in `StatusIncidentMeta.astro`, the status UI/browser DTO architecture guard, timeline tooltip DTO serialization/parsing, status public DTO adapter, discovery schema boundary, and status Markdown/llms surfaces. Static search found no raw status UI tokens in `apps/www/src/components/status` or `apps/www/src/pages/status`; raw snake_case remains only in allowed raw schema/mapper/tests, legacy public DTO/schema, and public documentation text. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built harness confirming `/status/data/status.json` keeps legacy public `started_at`/`started_has_time`/`is_active`/`source_url` without camelCase public leaks while `/status/index.html` uses camelCase tooltip DTO keys and does not contain rejected raw UI tokens. No blocking findings; publish `review.passed`.

Finalizer Step 9: re-read migration context/plan/progress, Task 09, ADR-013, ADR-011, app rules, and runtime task `task-1779351581-a498`. Confirmed the rejection fix is represented in the status UI/browser DTO guard and `StatusIncidentMeta.astro` now uses `sourceUrl` and `appliesToAllAreas`. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built status JSON/HTML harness confirming 18 incidents, legacy public `started_at`/`started_has_time`/`is_active`/`source_url`, no camelCase public JSON leaks, and camelCase tooltip DTO keys without raw status UI token leaks. Steps 10-13 remain incomplete, so close Step 9 runtime task and publish `queue.advance` rather than `LOOP_COMPLETE`.

Planner continuation after Step 9 finalization: ready queue was empty and Step 9 runtime work was closed. Advanced `progress.md` to Step 10 - People raw/domain foundation, ensured current-wave runtime task `task-1779352900-4579` with key `code-assist:raw-to-domain-data:step-10:people-raw-domain-foundation`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/10-people-raw-domain-foundation.md`.

Builder Step 10: started runtime task `task-1779352900-4579`, read ADR-013/ADR-011, migration context/plan/progress, app rules, and the Step 10 artifact. Added RED coverage for reusable people raw schema, raw-to-domain mapper, camelCase people dataset/profile/backlink fields, and an architecture guard covering people loader/registry/view/mention refs/profile page. Implemented `src/lib/people/raw-schema.ts`, `types.ts`, and `mapper.ts`; routed `content.config.ts` through `RawPersonProfileSchema`; moved contact normalization to the raw/domain mapper; and migrated people internals to `bySlug`, `mentionRegistry`, `nameCases`, `markdownUrl`, `sourceId`, `htmlUrl`, `mentionedAt`, and `sortKey` while keeping public discovery/llms DTO snake_case contract names. Code-simplification pass found no smaller safe change beyond keeping raw reads in mapper/raw schema and preserving `schema.ts` as the compatibility constants/type re-export module. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts src/lib/people/schema.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`. Recorded fix memories `mem-1779353521-7b16` and `mem-1779353574-7720`.

Critic Step 10 review: re-checked runtime task `task-1779352900-4579`, ADR-013, ADR-011, migration context/progress, app rules, and the Step 10 artifact. Inspected people raw schema, handwritten readonly domain types, raw-to-domain mapper, loader/registry, people mention refs/view/profile page, and public discovery/llms DTO boundaries. Static search confirmed direct raw people property access remains only in raw schema/mapper/tests or explicit public DTO/documentation surfaces; internal people dataset/profile/backlink contracts use `bySlug`, `mentionRegistry`, `nameCases`, `markdownUrl`, `sourceId`, `htmlUrl`, `mentionedAt`, and `sortKey`. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts src/lib/people/schema.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built people JSON/HTML/Markdown harness confirming 3 profiles, legacy public `html_url`/`markdown_url`/`body_markdown`/`mention_count`/`backlink_count`, no camelCase public JSON leaks, no rejected raw people tokens in the built profile HTML, and a valid profile Markdown heading/contacts. Recorded resolved harness assertion memory `mem-1779353841-41bd`. No blocking findings; publish `review.passed`.

Finalizer Step 10: re-read migration context/plan/progress, Task 10, ADR-013, ADR-011, app rules, and runtime task `task-1779352900-4579`. Confirmed `RawPersonProfileSchema`, handwritten readonly people domain types, raw-to-domain mapper/contact normalization, people loader/registry/view/profile page internals, and the architecture guard satisfy Step 10 acceptance. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts src/lib/people/schema.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built people JSON/HTML/Markdown harness confirming 3 profiles, legacy public `html_url`/`markdown_url`/`body_markdown`/`mention_count`/`backlink_count`, no camelCase public JSON leaks, no raw people tokens in profile HTML, and a valid profile Markdown heading/contacts. Steps 11-13 remain incomplete, so close Step 10 runtime task and publish `queue.advance` rather than `LOOP_COMPLETE`.
Planner continuation after Step 10 finalization: ready queue was empty and Step 10 runtime work was closed. Advanced `progress.md` to Step 11 - People public surfaces and backlinks, ensured current-wave runtime task `task-1779354074-8c97` with key `code-assist:raw-to-domain-data:step-11:people-public-and-backlinks`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/11-people-public-and-backlinks.md`.

Builder Step 11: started runtime task `task-1779354074-8c97`, read ADR-013/ADR-011, migration context/plan/progress, app rules, and the Step 11 artifact. Added RED public DTO coverage for preserved legacy people JSON keys and the domain mention graph -> people backlinks adapter. Implemented `src/lib/people/public-dto.ts`, moved people public payload DTOs/builders out of `discovery.ts`, routed `buildPeoplePayload` through `buildPeoplePublicPayload`, and moved backlink graph conversion out of `load.ts` behind `createPeopleBacklinksFromGraph`. Code-simplification pass removed the stale `absoluteUrl` import and updated the llms fixture to camelCase domain backlink fields. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and built people JSON harness confirming 3 profiles, legacy public snake_case keys, and no camelCase public JSON leaks. Recorded fix memories `mem-1779354251-817c`, `mem-1779354355-d9e6`, and `mem-1779354472-7eaf`.

Critic Step 11 review: re-checked runtime task `task-1779354074-8c97`, ADR-013, ADR-011, migration context/progress, app rules, and the Step 11 artifact. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built people JSON harness confirming 3 profiles, legacy public `html_url`/`markdown_url`/`name_cases`/`source_id`, no camelCase public JSON leaks, and populated backlink links. Review rejects because `apps/www/src/lib/people/load.ts` now imports `createPeopleBacklinksFromGraph` from `apps/www/src/lib/people/public-dto.ts`; this makes the internal domain loader depend on the public DTO module, which violates the ADR-013 dependency direction where public adapters import domain interfaces rather than domain loaders importing public adapters. Builder should move the graph-to-domain backlinks adapter to an internal people module such as `backlinks.ts` or `mention-backlinks.ts`, keep `public-dto.ts` limited to domain -> public payload DTO conversion, and extend a guard/test so `load.ts` cannot import `./public-dto`.

Builder Step 11 rejection fix: added a RED guard in `load.test.ts` proving `load.ts` must not import `./public-dto`; it failed on the rejected dependency as expected. Moved `createPeopleBacklinksFromGraph` and graph-ref filtering helpers into internal `src/lib/people/backlinks.ts`, changed `load.ts` to import that internal module, and kept `public-dto.ts` focused on domain -> legacy public payload conversion. Code-simplification pass split the backlink adapter test into `backlinks.test.ts` so `public-dto.test.ts` only covers public payload DTO behavior. Verification passed: focused backlink/public DTO/loader tests, full people tests, `@shelkovo/www` typecheck, `@shelkovo/www` build, prettier check for changed files, and a built people JSON contract harness.

Critic Step 11 re-review: re-checked runtime task `task-1779354074-8c97`, ADR-013, ADR-011, migration context/progress, app rules, and the Step 11 artifact. Inspected `backlinks.ts`, `public-dto.ts`, `load.ts`, discovery re-exports, people llms/markdown/profile surfaces, and architecture guards. Confirmed `load.ts` imports the graph-to-domain adapter from internal `./backlinks`, not `./public-dto`; `public-dto.ts` only converts domain people profiles/backlinks to the legacy public JSON DTO; internal people contracts use camelCase. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts src/lib/people/public-dto.test.ts src/lib/people/backlinks.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, prettier check for changed Step 11 files, built people JSON harness confirming 3 profiles, 3 backlinks, legacy `html_url`/`markdown_url`/`name_cases`/`source_id`, no camelCase public JSON leaks, and built profile Markdown harness confirming backlinks render without raw JSON keys. Recorded resolved harness assumption memory `mem-1779355353-67e9`. No blocking findings; publish `review.passed`.
Finalizer Step 11: re-read migration context/plan/progress, Task 11, ADR-013, ADR-011, app rules, and runtime task `task-1779354074-8c97`. Confirmed the rejection fix is represented in code and tests: `load.ts` imports `createPeopleBacklinksFromGraph` from internal `./backlinks`, `public-dto.ts` only converts domain people profiles/backlinks into the legacy public people payload, and `load.test.ts` guards against importing `./public-dto`. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, Step 11 prettier check, and a built people JSON/Markdown harness confirming 3 profiles, 3 backlinks, legacy public `html_url`/`markdown_url`/`name_cases`/`source_id`, no camelCase public JSON leaks, contacts, and rendered backlink Markdown. Steps 12-13 remain incomplete, so close Step 11 runtime task and publish `queue.advance` rather than `LOOP_COMPLETE`.

Planner continuation after Step 11 finalization: ready queue was empty and Step 11 runtime work was closed. Advanced `progress.md` to Step 12 - Public DTO contracts across sections, ensured current-wave runtime task `task-1779355664-cde2` with key `code-assist:raw-to-domain-data:step-12:public-dto-contracts`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/12-public-dto-contracts.md`.

Builder Step 12: started runtime task `task-1779355664-cde2`, read ADR-013/ADR-011, migration context/plan/progress, app rules, and the Step 12 artifact. Added a RED root discovery guard requiring explicit public DTO/serializer boundaries for compare, news, status and people plus one canonical inventory section for intentional legacy public fields; the guard failed on the missing inventory section as expected. Added `Intentional legacy public fields` to `docs/migrations/raw-to-domain-data/inventory.md`, covering compare full JSON legacy keys/enums, news JSON keys, status JSON keys, people JSON keys and public Markdown companion frontmatter keys. Code-simplification pass kept the scope to the guard and inventory because existing public adapters/discovery schemas already represent the current contracts. Verification passed: focused root discovery test, full Step 12 test command, `@shelkovo/www` typecheck, prettier check for changed files, and `@shelkovo/www` build. Recorded expected RED failure memory `mem-1779355860-e99a`.

Critic Step 12 review: re-checked runtime task `task-1779355664-cde2`, ADR-013, ADR-011, migration context/progress, app rules, and the Step 12 artifact. Inspected the new root discovery guard, the inventory `Intentional legacy public fields` section, compare full/explorer serializers, and news/status/people public DTO adapters/discovery re-exports. Confirmed public JSON routes go through explicit public payload adapters/serializers and legacy snake_case fields are intentionally listed in one inventory section rather than leaking from domain models accidentally. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/root-discovery-routes.test.ts src/lib/news src/lib/status src/lib/people src/compare/lib`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, Step 12 prettier check, and a built JSON harness confirming compare full legacy `short_name`, compare explorer camelCase `shortName`, news `published_at`/`html_url`, status `started_at`/`is_active`, and people `profile_count`/`html_url`. No blocking findings; publish `review.passed`.

Finalizer Step 12: re-read migration context/plan/progress, Task 12, ADR-013, ADR-011, app rules, inventory, root discovery guard, and runtime task `task-1779355664-cde2`. Confirmed Step 12 satisfies the public DTO contract pass: compare/news/status/people public JSON routes have explicit public adapters or serializers, root discovery guard covers those boundaries, and `inventory.md` has one `Intentional legacy public fields` section documenting representative legacy `snake_case` public contracts. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/root-discovery-routes.test.ts src/lib/news src/lib/status src/lib/people src/compare/lib`, `pnpm --filter @shelkovo/www typecheck`, Step 12 prettier check, `pnpm --filter @shelkovo/www build`, built DTO harness confirming compare full legacy `short_name`, compare explorer `shortName`, news `published_at`/`html_url`/`markdown_url`, status `started_at`/`is_active`/`source_url`, people `stats.profile_count`/profile `html_url`/`markdown_url`, and an adversarial built JSON leak check confirming legacy feeds do not expose selected camelCase domain keys. Recorded resolved harness-shape memory `mem-1779356348-7796`. Step 13 remains incomplete, so close Step 12 runtime task and publish `queue.advance` rather than `LOOP_COMPLETE`.

Planner continuation after Step 12 finalization: ready queue was empty and Step 12 runtime work was closed. Advanced `progress.md` to Step 13 - Final guardrails and cleanup, ensured current-wave runtime task `task-1779356478-501f` with key `code-assist:raw-to-domain-data:step-13:final-guardrails`, and prepared to emit `tasks.ready` for Builder with artifact `docs/migrations/raw-to-domain-data/tasks/13-final-guardrails.md`.
Critic Step 13 review: re-read runtime task `task-1779356478-501f`, Step 13 artifact, migration plan/progress/CONTEXT, ADR-013, ADR-011, and app rules. Inspected the new `raw-domain-boundary.architecture.test.ts`, compare compatibility cleanup in `schema.ts`/`schema.test.ts`, inventory, progress, CONTEXT, and `apps/www/AGENTS.md`. Focused verification `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts` passed, but the review rejects: the final guardrail globally allowlists raw-like tokens such as `source_url`, `started_at`, `active_incident`, and `no_incidents`, so it does not actually forbid raw `snake_case` in domain/internal files. There is already an internal status domain example: `StatusDaysWithoutIncidents.mode` in `src/lib/status/types.ts` is `'active_incident' | 'no_incidents'`, with internal producers/consumers in `load.ts` and `view.ts`; the Step 13 guard passes because those tokens are allowed. This misses the Task 13 acceptance criterion that final guardrails prove `snake_case` remains only in external/raw/public boundaries. Builder should remove broad token allowlisting for external field names, convert internal status mode values to camelCase or explicitly domain-style values behind public DTO mapping, and keep any unavoidable allowlist file/path-scoped with justification.

Builder Step 13 rejection fix: started runtime task `task-1779356478-501f`, re-read Step 13 docs, ADR-013/ADR-011, migration context/progress, and app rules. RED changed `raw-domain-boundary.architecture.test.ts` to remove the global raw-token allowlist and allow only file-scoped exceptions with justification; focused guard failed on internal status `active_incident`/`no_incidents`, sitemap `date_checked`, and known legacy browser/SEO/news raw-boundary tokens. GREEN converted internal status quiet-days mode values to `activeIncident`/`noIncidents`, mapped them back to legacy public `active_incident`/`no_incidents` in `status/public-dto.ts`, migrated compare sitemap metadata source shape to `dateChecked`, and kept remaining exceptions path-scoped. Code-simplification pass replaced a nested public DTO mode ternary with an explicit mapper. Verification passed: focused guard/status/sitemap tests, full `pnpm --filter @shelkovo/www test`, `pnpm --filter @shelkovo/www typecheck`, `pnpm build`, prettier check after formatting, and post-format focused tests plus typecheck.

Planner handling `review.fixed` for Step 13: runtime task `task-1779356478-501f` is still `in_progress`, progress/context already mark migration complete and Step 13 ready for closure after successful review. No implementation work should happen in this coordinator iteration. Emit exactly one `review.ready` event with the Step 13 task id/key/artifact and the builder rejection-fix summary so Critic can re-review the final guardrail fix.

Critic Step 13 re-review: re-read runtime task `task-1779356478-501f`, Step 13 artifact, migration progress/CONTEXT, ADR-013, ADR-011, and app rules. Inspected `raw-domain-boundary.architecture.test.ts`, status quiet-days mode migration/public DTO mapping, sitemap `dateChecked`, and schema/type boundaries. Verification passed: `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts` and `pnpm --filter @shelkovo/www typecheck`. Adversarial boundary harness failed because `apps/www/src/lib/news/schema.ts`, `apps/www/src/lib/status/schema.ts`, and `apps/www/src/lib/people/schema.ts` still import/re-export domain types from `./types`; this violates ADR-013's dependency rule that schema files do not import domain interfaces and leaves temporary compatibility exports in place despite Step 13 acceptance. Recorded fix memory `mem-1779357759-d162`. Reject with instruction to remove those schema compatibility type re-exports, migrate consumers to import domain types from `types.ts` directly, and add/extend the final guardrail so schema files cannot import `./types`.

Builder Step 13 schema-boundary rejection fix: added a RED final guard proving `schema.ts` files must not import or re-export `./types`; it failed on news/status/people schemas as expected. Removed the temporary domain type compatibility exports from those schema modules, moved `EMPTY_PERSON_BACKLINKS` into `people/types.ts`, and migrated consumers to direct `types.ts` imports while keeping schema imports only for constants/raw helper aliases. Code-simplification pass found the narrowest readable shape was direct type imports plus the single guard. Verification passed: focused boundary/news/status/people/component tests, full `@shelkovo/www` test, `@shelkovo/www` typecheck, Step 13 prettier check, and `pnpm build`. Recorded RED/typecheck fix memories `mem-1779357882-2a97` and `mem-1779358110-fdcb`.
Critic Step 13 schema-boundary re-review: re-read runtime task `task-1779356478-501f`, Step 13 artifact, migration progress/CONTEXT, ADR-013, ADR-011, and app rules. Inspected `raw-domain-boundary.architecture.test.ts`, `news/schema.ts`, `status/schema.ts`, `people/schema.ts`, `people/types.ts`, status mode mapping, and direct consumers. Confirmed schemas no longer import/re-export `./types`, `EMPTY_PERSON_BACKLINKS` lives in `people/types.ts`, domain consumers import from `types.ts`, and status internal quiet-days mode uses `activeIncident`/`noIncidents` with legacy public mapping only in `status/public-dto.ts`. Verification passed: runtime task show, `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/news src/lib/status src/lib/people src/components/news src/components/status`, `pnpm --filter @shelkovo/www typecheck`, `pnpm build`, and a refined adversarial boundary harness checking schema imports, status raw-mode leaks, and direct domain type imports from schema. Two initial adversarial harness attempts failed due shell escaping and over-broad regex false positives; recorded fix memories `mem-1779358539-50f4` and `mem-1779358539-787b`. No blocking findings; publish `review.passed`.
