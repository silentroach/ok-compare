# UI/UX Follow-Ups

Выбранные пункты из аудита страниц на `localhost:4321` для последующего разбора.

Актуальных открытых пунктов нет.

## Закрыто 2026-05-11

- `8. Event-карточки грузят тяжелые iframe-карты без title`: фоновым iframe Яндекс.Карт добавлен `title`; прежняя карта и позиционирование маркера сохранены, а явная ссылка `Открыть на Яндекс Картах` осталась в действиях карточки.
- `9. Глобальный smooth scroll не уважает reduced motion`: для `prefers-reduced-motion: reduce` глобальный `scroll-behavior` сбрасывается в `auto`.
- `15. Две meta-строки выходят за локальные лимиты`: для затронутых новостей добавлены или уточнены `seo.title` и `seo.description`, основной `summary` не сокращался.
