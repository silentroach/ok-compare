# Extra Notes

- `waste.pdf` has 13 PDF pages. Dataset `source_refs.page` use PDF page numbers, not printed document page numbers 93-105.
- `waste.pdf` pages 1-5 cover `waste-operator-service`; pages 6-13 cover `waste-transfer-from-homes`.
- In `waste.pdf`, the regional-operator amount is bucketed inconsistently: pages 3-4 classify `7 623 207,58` as third-party services, while page 5 classifies rounded `7 623 208` as material resources. The dataset keeps contractor resources and marks the material bucket conflict as `needs_check`.
- For `waste-transfer-from-homes`, page 11 has direct `НДС 5% 597 612,91`, but `estimate-2026` annual gross `12 851 178` reconciles with page 13 income `12 239 217` plus derived 5% VAT `611 960,85`. The dataset marks this gross/VAT reconciliation as `needs_check`.
- `security.pdf` has 13 PDF pages. Dataset `source_refs.page` use PDF page numbers, not printed document page numbers 106-118.
- `security.pdf` page 1 confirms periodic patrol/rounds as part of CHOP access control, but does not provide route length or patrol frequency. The full-reglament layer has these facts in `/815/regulation/assets/`: `security-patrol-route-km` totals `7` km and `security-patrol-times-per-day` is `5` times/day by village; `/815/regulation/services/` only states `year-round-access-control` frequency as `Круглосуточно`. Do not copy these full-layer facts into detail data sourced from `security.pdf`.
- For security rows, page 12 has direct local-calculation `НДС 5% 697 771,79`, but `estimate-2026` annual gross `14 752 949` reconciles with page 13 income `14 050 428` plus derived 5% VAT `702 521,40`. Keep this as a `needs_check` note until the source of final VAT is confirmed.
- `security.pdf` shows `УСН 94 992` and VAT only at whole-service level, not per row. Row-level USN/VAT allocations in `estimate-details-2026` are derived from `estimate-2026` gross rows and must stay marked as `needs_check`.
- `lighting.pdf` has 14 PDF pages. Dataset `source_refs.page` use PDF page numbers, not printed document page numbers 79-92.
- `lighting.pdf` page 12 has direct local-calculation `НДС 5% 547 489,58`, but `estimate-2026` lighting gross `11 738 585` reconciles with page 14 income `11 179 605` plus derived 5% VAT `558 980,25`. Row-level VAT allocations in `estimate-details-2026` use the derived path and stay marked as `needs_check`.
- `lighting.pdf` shows `УСН 229 813` only at whole-service level, not per row. Row-level USN allocations are derived from `estimate-2026` gross rows and the total calculation; the derived row USN values sum to `229 813,07` because of row rounding, and this rounding is accepted.
- For lighting pole painting, `/815/regulation/assets` has `electric-lighting-poles-metal` total `1416,8` м², while `lighting.pdf` page 2 calculates painting as `2 833,60` м² because it is two layers. The paint amount itself is only in `lighting.pdf`: `453,4` kg at `612,50` rub/kg with total `277 692,80`; the displayed one-decimal quantity is rounded, so the dataset keeps the source total and notes hidden precision near `453,376` kg.
- In `lighting.pdf`, page 13 resource statement rounds some quantities more aggressively than local rows: `Электромонтажник` `5767,1` чел-час, paint `453` кг. and electricity `218457` кВт\*час. Structured quote items keep these displayed quantities and source row totals, with rounding notes instead of deriving hidden quantities from totals.
- `landscaping.pdf` has 22 PDF pages. Dataset `source_refs.page` use PDF page numbers, not printed document page numbers 39-60.
- `landscaping.pdf` page 20 has direct local-calculation `НДС 5% 482 298,47`, but `estimate-2026` landscaping gross `10 218 079` reconciles with row-level gross values derived from aggregate rows; page 22 income `9 731 505` plus 5% VAT would give `10 218 080,25`. Keep this as a note on derived row-level VAT allocations.
- `landscaping.pdf` shows `УСН 85 534` only at whole-service level, not per row. Row-level USN allocations in `estimate-details-2026` are derived from `estimate-2026` gross rows and sum to `85 534,40`; this is accepted as row rounding.
- In `landscaping.pdf`, detailed tree/shrub labor resources sum to `415 902,92`, while the section and `estimate-2026` bucket show `415 902,91`; this is accepted as row rounding, and the dataset keeps individual source rows with a `0,02` rub tolerance for that primary-salary control.
- `landscaping.pdf` has more granular frequencies than the aggregate `estimate-2026` rows: mowing `15`, tree/shrub subworks `1/2/3/5/20`, hogweed `3`, forest care `1`. Keep these frequencies marked in work item notes instead of collapsing them to one row-level frequency.
- `improvement.pdf` has 18 PDF pages. Dataset `source_refs.page` use PDF page numbers, not printed document page numbers 61-78.
- `improvement.pdf` page 15 local total `4 363 835,75` combines objects maintenance and perimeter-fence repair. For row-level reconciliation with `estimate-2026`, dataset splits material `298 320,00` from fence repair into `improvement-road-surface-repair` and keeps the remaining object materials as `56 347,20`.
- In `improvement.pdf`, page 16 direct local-calculation `НДС 5% 218 191,79` is 5% of `4 363 835,75` before УСН. Aggregate rows reconcile with derived VAT from row gross values: `4 366 756` for objects and `320 424` for the disputed repair line; together they sum to `4 687 180`, while the section line in `final.pdf` shows `4 687 181` because of whole-ruble section rounding.
- `improvement.pdf` page 18 shows `УСН 100 146` only at whole-service level, not per row. Row-level USN allocations in `estimate-details-2026` are derived from `estimate-2026` gross rows: `93 299,49` for objects and `6 845,71` for the disputed repair line.
- In `improvement.pdf`, resource-statement text extraction on page 17 shows suspicious units for `Лопата совковая` and `Тачка садовая`; the local calculation on page 14 shows these resources as `шт.`, so the dataset keeps `шт.` and preserves source totals.
- `cleaning.pdf` has 27 PDF pages. Dataset `source_refs.page` use PDF page numbers, not printed document page numbers 12-38.
- In `cleaning.pdf`, winter mechanized section totals are on PDF page 13. Existing aggregate source refs may point at page 12, but page 12 only contains the СИЗ position ending; keep detail source refs on page 13 for the section total.
- For `cleaning-winter-mechanized`, PDF page 25 has direct local-calculation `НДС 5% 7 441 315,26`, but aggregate cleaning totals reconcile with page 27 income `151 439 865` plus derived 5% VAT. Row-level VAT for winter mechanized cleaning is derived from `estimate-2026` and stays marked as `needs_check`.
- `cleaning.pdf` shows `УСН 2 613 560` only at whole-service level, not per row. Row-level USN for `cleaning-winter-mechanized` is derived from the aggregate row and the page 13 section total.
- User confirmed: page 13 is the correct page for the winter mechanized section total; page 12 contains only intermediate totals for preceding positions.
- User confirmed: derived row-level VAT/USN for `cleaning-winter-mechanized` is acceptable, but should remain documented as a derived reconciliation note.
- User confirmed: rounding of winter mechanized sand from `73,6 т` in the production program to `74 т` in the local estimate row is acceptable.
- For `cleaning-winter-manual`, the production program spans PDF pages 2-4; the local estimate rows are on PDF pages 13-17, and the section totals are on PDF page 17.
- For `cleaning-winter-manual`, the local estimate displays rounded labor, sand, PPE and inventory quantities; the dataset keeps production-program quantities where available, local money totals, and explicit rounding notes.
- Row-level USN/VAT for `cleaning-winter-manual` are derived from `estimate-2026`: income `2 989 256,19`, USN `51 588,99`, VAT `149 462,81`. VAT/gross remain `needs_check` because `cleaning.pdf` has the same document-level VAT conflict as `cleaning-winter-mechanized`.
- User confirmed: small norm/quantity rounding in `cleaning-winter-manual` is acceptable when the difference is minor.
- User confirmed: keep the `cleaning-winter-manual` derived VAT/USN discrepancy documented as a note, consistent with the other cleaning rows.
- Для `cleaning-summer-mechanized` производственная программа содержит ключевую строку полива на PDF page 4: `Дороги (асфальт). Полив водой (обеспыливание) - 3 раза в день без дождя`; детальная кратность `234`, машино-часы трактора/OPM-5 `22389`, вода `9568,0 м³`. User confirmed: `153 раз/год` в агрегированной строке `estimate-2026` относится к летней уборке территории в целом, а `234` нужно оставить как частоту полива. Почему расчетный полив чаще и почему эти частоты отличаются, пока непонятно; держать это заметкой в `estimate-details-2026` и не менять агрегированную строку.
- Для `cleaning-summer-mechanized` строки локальной сметы находятся на PDF pages 18-19, итог раздела — на PDF page 19: прямые затраты `56 728 492,34`, машины `35 473 842,02`, зарплата машинистов `20 918 659,44`, материалы `335 990,88`, итог до строковых УСН/НДС `86 056 452,88`.
- Строковые USN/VAT для `cleaning-summer-mechanized` рассчитаны из `estimate-2026`: доход `87 567 702,86`, USN `1 511 249,98`, VAT `4 378 385,14`, gross `91 946 088`. VAT/gross остаются `needs_check`, потому что в `cleaning.pdf` есть тот же конфликт НДС на уровне документа, что и в остальных строках уборки.
- User confirmed: для `cleaning-summer-mechanized` строковые USN/VAT оставить как обычно, с заметкой о производной сверке и конфликте НДС на уровне документа.
- Для `cleaning-summer-manual` производственная программа находится на PDF pages 5-6: очистка кромки вдоль бортового камня `153`, уборка случайного мусора на парковочных/иных площадках `153`, подметание парковочных/иных площадок `22`, очистка контейнерной площадки `153`, очистка открытых ливневых траншей `15 раз в летний период`, СИЗ и инвентарь.
- Для `cleaning-summer-manual` строки локальной сметы находятся на PDF pages 20-24, итог раздела — на PDF page 24: прямые затраты `16 703 903,28`, основная зарплата `16 429 653,29`, материалы `274 250,00`, страховые взносы `4 961 755,30`, общеэксплуатационные расходы `11 500 757,30`, прибыль `6 571 861,32`, итог до строковых УСН/НДС `39 738 277,20`.
- В `cleaning-summer-manual` сумма округленных трудовых ресурсов дает `16 429 653,28`, а итог раздела показывает `16 429 653,29`; user confirmed: такое копеечное округление не страшно. Сумма bucket-ов `estimate-2026` до УСН/НДС дает `39 738 277,21`, на `0,01` выше итога раздела PDF; user confirmed: это тоже не страшное округление.
- Строковые USN/VAT для `cleaning-summer-manual` рассчитаны из `estimate-2026`: доход `40 436 126,67`, USN `697 849,46`, VAT `2 021 806,33`, gross `42 457 933`. VAT/gross остаются `needs_check`, потому что в `cleaning.pdf` есть тот же конфликт НДС на уровне документа, что и в остальных строках уборки; user confirmed: ok.
- Для `cleaning.pdf` ресурсная ведомость находится на PDF page 26. Она сверяет документные ресурсные суммы: рабочие `26 563,3 чел-час` / `17 641 901,84`, машинисты `27 739,3 чел-час` / `25 917 429,40`, машины `43 379 375,72`, материалы `817 415,84`.
- В `cleaning.pdf` ресурсная ведомость показывает годовые количества с большей точностью, чем часть локальных строк: например, трактор `27 739,3` против суммы округленных строк `27 739`, разбрасыватель `210,4` против `210`, ОПМ-5,0 `22 389,2` против `22 389`; рублевые итоги сходятся, поэтому это документируется как rounding note.
- В `cleaning.pdf` page 26 единица песка в ресурсной ведомости извлекается как `кг.`, но производственная программа и локальные позиции используют песок в тоннах при цене `1800,00`; dataset сохраняет `т.` в ресурсах и добавляет `needs_check` на контроль материалов ресурсной ведомости.
- Для document-level control totals ресурсной ведомости уборки dataset использует section id `cleaning` в `estimate_row_id`; тесты принимают ID строк и разделов из `estimate-2026`.
- Идея по структурированию многострочных `source_refs.quote` вынесена в Task 019: добавить optional item arrays рядом с текущим текстовым `quote` без поломки JSON-контракта.
- `final.pdf` содержит 2 PDF-страницы. Dataset `source_refs.page` использует PDF-страницы, а не печатные номера страниц 125-126.
- `final.pdf` используется только как верхнеуровневый gross-контроль: он связывает строки и разделы с `estimate-2026`, но не заменяет detail-контроли секционных PDF и суммы ресурсов.
- Final-PDF controls нашли три рублевых расхождения округления между разделом и суммой строк: `cleaning` в final section `159 011 858` против суммы строк `159 011 859`; `improvement` в final section `4 687 181` против суммы строк `4 687 180`; `security` в final section `14 752 949` против суммы строк `14 752 950`. Они сохранены как `needs_check` final controls в checks markdown.
- Task 019 добавил optional `source_refs[].quote_items` рядом с прежним `quote`; первичная миграция сделана во всех секционных detail-модулях. Тест отдельно проверяет, что очевидные многострочные ресурсные цитаты по СИЗ, инвентарю, труду, машинам и ресурсным ведомостям не остаются без `quote_items`.
- `quote_items` уже поддерживают табличные поля источника: единица измерения хранится в `quantity.unit`, количество в `quantity.value/raw`, цена за единицу в `unit_price_rub`, сумма в `total_rub`. Первый проход Task 019 местами заполнил только название и сумму; полное source-driven обогащение по PDF вынесено в Tasks 020-025 по секциям, чтобы не выводить количество и цену из итогов без проверки исходной строки.
- User suggested that `source_refs[].quote_items[].quote` and `raw` fragments inside structured values are likely not useful for users or LLM consumers. Added Task 026 to review and remove these public-contract fields after Tasks 020-025, while preserving source provenance through `source_refs[].quote`, PDF, page and fragment.
- В `waste.pdf` page 8 нормативное штатное расписание показывает для рабочего и машиниста значения из колонки `Всего, руб. ((гр. 5 + гр. 6 + гр. 7 + гр. 8) × гр. 4)`: `1 726,78` и `654,02`. Это не годовые ресурсные суммы; годовые суммы труда остаются из локального расчета и ресурсной ведомости pages 9-12.
- В `landscaping.pdf` page 21 ресурсная ведомость показывает `Триммер бензиновый` как `1030,4 маш.-час`, а локальная строка page 9 округляет количество до `1030`; рублевый итог совпадает, поэтому quote item ресурсной ведомости хранит `1030,4`, а основной resource сохраняет локальную строку `1030`.
