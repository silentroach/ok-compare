# Калькулятор тарифа по смете 2026

Статус: продуктовая спецификация для реализации.
Дата: 2026-05-07.
Зона реализации: `apps/www`, новый публичный раздел по смете регламента.

## Проблема

Смета регламента ОК читается как набор PDF-таблиц, где важные величины смешаны с нормативными основаниями, ресурсными ведомостями и техническими строками. Собственнику трудно понять, какие параметры формируют тариф `902,07 ₽/сотка/мес`, и что изменится, если оспорить частоту работ, объем, ставку или подрядную цену.

Нужно сделать нейтральный интерактивный калькулятор: вся смета видна на странице, пользователь может менять значимые поля и сразу видеть новый тариф за сотку в месяц.

## Рекомендованное направление

Делать не PDF-таблицу и не разоблачительный материал, а аккуратный финансовый инструмент. Главная единица интерфейса - `₽/сотка/мес`, а не годовая сумма и не цена участка пользователя. Годовые суммы нужны в данных, деталях и проверках, но не должны быть главным визуальным числом.

Основной экран строится из последовательных секционных таблиц: `Перемещение мусора`, `Уборка территории`, `Озеленение`, `Благоустройство`, `Охрана`, `Организация работы с РО по вывозу мусора`, `Освещение и электроснабжение`. Дерево секций не нужно: оно добавит когнитивной вложенности. Каждая секция сама является плоским блоком с таблицей и компактным итогом секции.

Тон страницы максимально нейтральный. Допустимы только фактические бейджи вроде `крупная статья`, `подрядная услуга`, `ручной труд`, `машино-часы`, `требует проверки`. Саркастические подписи и оценочные формулировки не использовать.

## Источники

- `docs/reglament/original/final.pdf` - итоговая публичная смета, источник официального общего тарифа и секционных итогов.
- `docs/reglament/original/cleaning.pdf` - детализация уборки территории.
- `docs/reglament/original/landscaping.pdf` - детализация озеленения.
- `docs/reglament/original/improvement.pdf` - детализация благоустройства.
- `docs/reglament/original/lighting.pdf` - детализация освещения и электроснабжения.
- `docs/reglament/original/security.pdf` - детализация охраны.
- `docs/reglament/original/waste.pdf` - детализация организации вывоза и перемещения мусора.

## Базовые числа

- Тарифицируемая площадь: `20 440,54 сотки`.
- Официальный итог: `221 264 198 ₽/год`.
- Официальный тариф: `902,07 ₽/сотка/мес`.
- Формула тарифа: `годовой итог / 20 440,54 / 12`.

Важная деталь: `final.pdf` сходится не с локальной строкой `Сметная стоимость`, а с полной строкой `Доходов всего` из калькуляции, умноженной на НДС 5%. Например, для уборки территории: `151 439 865 × 1,05 = 159 011 858`. Поэтому расчетная модель должна учитывать не только прямые затраты, страховые, общеэксплуатационные и прибыль, но и налог по УСН внутри расчетной прибыли, а затем НДС 5%.

## Расчетная модель

Глобальные параметры по умолчанию:

| Параметр                     |          Значение | Комментарий                           |
| ---------------------------- | ----------------: | ------------------------------------- |
| Площадь тарификации          | `20 440,54 сотки` | из `final.pdf`                        |
| Страховые взносы             |           `30,2%` | от ФОТ                                |
| Общеэксплуатационные расходы |             `70%` | от ФОТ                                |
| Прибыль                      |             `40%` | от ФОТ                                |
| УСН                          |             `15%` | от прибыли, остающейся в распоряжении |
| НДС                          |              `5%` | применяется к доходам всего           |

Базовая формула для позиции:

```text
fot = основная зарплата + зарплата машинистов
direct = fot + машины + материалы + подрядчики
insurance = fot * 0.302
overhead = fot * 0.70
profit = fot * 0.40
usn = profit * 0.15
income = direct + insurance + overhead + profit + usn
gross = income * 1.05
tariff = gross / 20 440,54 / 12
```

Для подрядных или материальных позиций без ФОТ страховые, общеэксплуатационные, прибыль и УСН обычно равны нулю. Это поведение должно быть задано явно в данных позиции, а не угадываться только по нулевым значениям.

## Модель данных

Данные не парсить из PDF на клиенте. PDF-парсинг слишком нестабилен для таблиц такого качества. Нужен нормализованный статический слой в `apps/www/src/data/reglament/estimate-2026.ts` или аналогичном файле.

Минимальная структура сущностей:

| Сущность            | Смысл                                                                       |
| ------------------- | --------------------------------------------------------------------------- |
| `Estimate`          | год, площадь, глобальные коэффициенты, секции, официальный итог             |
| `EstimateSection`   | название секции, итог из `final.pdf`, список строк                          |
| `EstimateRow`       | пользовательская строка работы или сгруппированная подстрока                |
| `EstimateSourceRef` | PDF, страница, фрагмент/закладка, опциональная заметка                      |
| `EditableField`     | поле, которое можно менять в UI                                             |
| `CostBreakdown`     | ФОТ, машины, материалы, подрядчики, страховые, накладные, прибыль, УСН, НДС |

Каждая строка должна иметь стабильный `id`, человекочитаемое название, исходные значения, формулу пересчета, ссылки на источник и набор редактируемых полей. Служебные ресурсные строки вроде спецодежды, мыла и инвентаря можно группировать под родительской строкой, если отдельное редактирование не дает пользователю понятной ценности.

## UI

### Верх страницы

- Заголовок: `Калькулятор тарифа по смете 2026`.
- Короткое объяснение: это интерактивная версия сметы регламента, итог считается в `₽/сотка/мес`.
- Компактная итоговая панель: официальный тариф, текущий тариф, разница.
- Поле `мой участок` в текущий продукт не добавлять: калькулятор остается инструментом проверки тарифа в `₽/сотка`, а не расчета частного платежа пользователя.

### Sticky-панель

Sticky должна быть супер-компактной.

Десктоп:

```text
902,07 ₽/сотка/мес -> 861,20 ₽    -40,87 ₽    Сбросить
```

Мобайл:

```text
861,20 ₽/сотка/мес   -40,87 ₽   Сброс
```

Панель не должна превращаться в карточку-дашборд. Это строка текущего состояния, а не отдельный экран.

### Секционные таблицы

Каждая секция рендерится отдельной таблицей с компактным итогом секции над таблицей или в первой строке секции.

Основные колонки:

| Колонка       | Смысл                                                                         |
| ------------- | ----------------------------------------------------------------------------- |
| Работа        | короткое название позиции                                                     |
| База          | объем или расчетная база, например `81 778 м²`, `506 светильников`, `4 поста` |
| Кратность     | количество раз/дней/месяцев в год                                             |
| Цена/ставка   | ставка, цена услуги или ставка ресурса                                        |
| Вклад в тариф | `₽/сотка/мес` по позиции                                                      |
| Изменение     | дельта к официальной строке после правок                                      |
| Детали        | раскрытие расчетного состава и источников                                     |

Годовую сумму не показывать как основную колонку. Ее можно показывать в раскрытии строки и отдавать в machine-readable data.

### Редактирование

В обычном режиме пользователь меняет только понятные поля: объем, кратность, цену/ставку, включение позиции. В деталях можно дать экспертные поля: трудозатраты, машино-часы, ФОТ, материалы, подрядчики и коэффициенты строки.

Все изменения должны пересчитываться мгновенно без запроса на сервер. Сброс должен возвращать официальную смету.

### Визуальный стиль

- Следовать `docs/design/design-code-shelkovo.md`.
- Почти белый фон, чистые белые поверхности, глубокий зеленый текст и действия.
- Не делать многоуровневые карточки, стеклянные панели, тяжелые тени, желтые/бумажные фоны.
- Таблицы должны быть опрятными и плотными, с четкими разделителями.
- Допустим один плоский контейнер на секцию, без вложенных островов и декоративной иерархии.

## Техническая архитектура

Предпочтительный вариант: Astro-страница с одним плоским клиентским контроллером на vanilla TypeScript. Не добавлять Svelte/React только ради калькулятора, если обычный DOM-контроллер справится.

Примерная структура:

| Файл                                                                 | Роль                               |
| -------------------------------------------------------------------- | ---------------------------------- |
| `apps/www/src/data/reglament/estimate-2026.ts`                       | нормализованные данные сметы       |
| `apps/www/src/lib/reglament/schema.ts`                               | типы и JSON Schema helpers         |
| `apps/www/src/lib/reglament/calculate.ts`                            | чистый расчет без DOM              |
| `apps/www/src/lib/reglament/discovery.ts`                            | JSON payload, API catalog, OpenAPI |
| `apps/www/src/lib/reglament/llms.ts`                                 | короткий и полный агентный обзор   |
| `apps/www/src/lib/reglament/routes.ts`                               | маршруты раздела                   |
| `apps/www/src/pages/reglament/index.astro`                           | HTML-страница калькулятора         |
| `apps/www/src/pages/reglament/index.md.ts`                           | markdown companion                 |
| `apps/www/src/pages/reglament/data/estimate-2026.json.ts`            | machine-readable data              |
| `apps/www/src/pages/reglament/llms.txt.ts`                           | короткий agent overview            |
| `apps/www/src/pages/reglament/llms-full.txt.ts`                      | полный agent overview              |
| `apps/www/src/pages/reglament/schemas/estimate-2026.schema.json.ts`  | JSON Schema                        |
| `apps/www/src/pages/reglament/openapi/estimate-2026.openapi.json.ts` | OpenAPI-like static description    |
| `apps/www/src/pages/reglament/.well-known/api-catalog.ts`            | RFC 9727 linkset                   |

Если интерактивность станет слишком сложной для DOM-контроллера, допустим один Svelte island верхнего уровня. Вложенные острова не использовать.

## Agent Discovery

AI-поверхности являются обязательной частью фичи, не постфактум.

Нужно отдать агентам:

- полный нормализованный список секций и строк;
- официальные значения и текущие формулы;
- все исходные поля, даже если UI показывает не все;
- источники по каждой строке: PDF, страница, закладка/секция;
- секционные итоги из `final.pdf`;
- вычисленные вклады в `₽/сотка/мес`;
- caveats о качестве PDF и необходимости проверки специалистом;
- JSON Schema и OpenAPI-like описание статического JSON;
- markdown companion для людей и LLM;
- обновление корневого `/.well-known/api-catalog`.

Публичные discovery-маршруты:

| Route                                           | Назначение                     |
| ----------------------------------------------- | ------------------------------ |
| `/reglament/`                                   | HTML-калькулятор               |
| `/reglament/index.md`                           | markdown companion             |
| `/reglament/data/estimate-2026.json`            | основной JSON feed             |
| `/reglament/llms.txt`                           | краткий обзор для агентов      |
| `/reglament/llms-full.txt`                      | полный обзор для агентов       |
| `/reglament/schemas/estimate-2026.schema.json`  | схема JSON feed                |
| `/reglament/openapi/estimate-2026.openapi.json` | описание статических endpoints |
| `/reglament/.well-known/api-catalog`            | section-level API catalog      |

## Не делаем сейчас

- Расчет платежа для конкретной площади участка.
- Сценарии с share-link.
- Авторизационные или пользовательские сохранения.
- Автоматический runtime-парсинг PDF.
- Саркастические комментарии внутри калькулятора.
- Полную копию исходных PDF-таблиц в UI.

## Открытые вопросы

- Финальный публичный route: `/reglament/` или `/tariff/`. Текущая рекомендация: `/reglament/`, потому что источник и раздел называются регламентом.
- Публиковать ли исходные PDF как статические файлы сайта. Если да, их нужно положить в `apps/www/public/reglament/original/` или добавить build-copy.
- Нужно ли в экспертном режиме редактировать глобальные коэффициенты, или достаточно редактировать строки.
- Насколько мелко дробить ресурсные строки спецодежды и инвентаря: отдельными строками в данных или только в деталях родительских работ.

## План реализации

### Task 1: Зафиксировать маршруты и типы раздела

Описание: создать фундамент раздела `reglament`: route helpers, TypeScript-типы сметы, базовые структуры для секций, строк, источников и редактируемых полей.

Acceptance criteria:

- [x] Есть `apps/www/src/lib/reglament/routes.ts` со всеми публичными маршрутами раздела.
- [x] Есть `apps/www/src/lib/reglament/schema.ts` с типами `Estimate`, `EstimateSection`, `EstimateRow`, `CostBreakdown`, `EditableField`, `EstimateSourceRef`.
- [x] Типы запрещают строку без `id`, `title`, `source_refs` и официального baseline.

Verification:

- [x] `pnpm --dir apps/www typecheck`

Dependencies: none.

Estimated scope: S.

### Task 2: Нормализовать baseline-данные из PDF

Описание: вручную перенести данные из `docs/reglament/original/*.pdf` в статический data module. UI может группировать ресурсные подстроки, но machine-readable payload должен сохранить достаточно деталей для проверки формул агентами.

Acceptance criteria:

- [x] В данных есть все секции из `final.pdf`.
- [x] Секционные итоги сходятся с `final.pdf`: `221 264 198 ₽` и `902,07 ₽/сотка/мес`.
- [x] Каждая пользовательская строка имеет source refs до PDF и страницы.
- [x] Все крупные строки из исходных PDF представлены явно, включая уборку, мусор, озеленение, благоустройство, охрану и освещение.
- [x] Служебные ресурсные строки либо представлены отдельно, либо явно привязаны к родительской строке.

Verification:

- [x] Unit-test проверяет общий итог и тариф.
- [x] Unit-test проверяет сумму секций против `final.pdf`.

Dependencies: Task 1.

Estimated scope: M.

### Task 3: Реализовать чистый расчетный движок

Описание: сделать `calculate.ts`, который пересчитывает строки, секции и общий тариф без DOM и без Astro. Это ядро должно быть тестируемым отдельно от UI.

Acceptance criteria:

- [x] `calculateEstimate()` принимает baseline и набор пользовательских изменений.
- [x] Поддержаны изменения объема, кратности, ставки, фиксированной цены и включения/выключения строки.
- [x] Расчет учитывает страховые, общеэксплуатационные, прибыль, УСН и НДС.
- [x] Возвращаются total gross, tariff per sotka per month, section totals, row deltas.

Verification:

- [x] Unit-test официального сценария возвращает `221 264 198 ₽` и `902,07 ₽/сотка/мес` с согласованной tolerances.
- [x] Unit-test изменения одной строки показывает корректную дельту строки, секции и общего тарифа.
- [x] `pnpm --dir apps/www test`

Dependencies: Task 2.

Estimated scope: M.

### Task 4: Собрать machine-readable discovery

Описание: создать JSON feed, JSON Schema, OpenAPI-like описание, section API catalog, markdown companion и llms outputs по аналогии с `news`, `status`, `people`.

Acceptance criteria:

- [x] `/reglament/data/estimate-2026.json` содержит baseline, формулы, секции, строки, source refs и computed values.
- [x] `/reglament/schemas/estimate-2026.schema.json` описывает payload без `additionalProperties` там, где структура стабильна.
- [x] `/reglament/openapi/estimate-2026.openapi.json` описывает статические endpoints раздела.
- [x] `/reglament/.well-known/api-catalog` возвращает linkset с JSON, schema, OpenAPI, llms и markdown companion.
- [x] `apps/www/src/lib/discovery.ts` включает новый раздел в корневой catalog.

Verification:

- [x] Tests для discovery проверяют наличие всех section routes.
- [x] Tests для root discovery проверяют, что `/reglament/` и JSON feed доступны агентам.
- [x] `pnpm --dir apps/www test`

Dependencies: Task 1, Task 2, Task 3.

Estimated scope: M.

### Task 5: Сделать статический каркас страницы

Описание: добавить HTML-страницу `/reglament/` без финального JS-интерактива, но уже с заголовком, вводным текстом, секционными таблицами, baseline-итогами и ссылками на источники.

Acceptance criteria:

- [x] Страница использует `BaseLayout` и правила `docs/page-meta.md`.
- [x] Title: `Смета 2026: калькулятор тарифа — Шелково Онлайн` или близкий вариант в пределах style guide.
- [x] Description коротко объясняет интерактивную смету и тариф за сотку.
- [x] Все секции видны на странице как отдельные таблицы.
- [x] Годовая цена не является основной колонкой.

Verification:

- [x] `pnpm --dir apps/www typecheck`
- [x] `pnpm --dir apps/www build`

Dependencies: Task 2.

Estimated scope: M.

### Task 6: Добавить один плоский клиентский контроллер

Описание: реализовать интерактивность без многоуровневых островов. Предпочтительно один vanilla TypeScript controller, который читает inputs, вызывает `calculateEstimate()` и обновляет DOM.

Acceptance criteria:

- [x] Изменение любого редактируемого поля мгновенно обновляет sticky total, секционный итог, вклад строки и дельту.
- [x] Кнопка `Сбросить` возвращает официальные значения.
- [x] На странице нет вложенных framework islands.
- [x] JS не нужен для чтения baseline-контента: без JS страница остается полезной статической таблицей.

Verification:

- [x] Unit-test для browser-independent state helpers, если они вынесены из DOM.
- [x] Manual check: поменять частоту/цену крупной строки и увидеть изменение тарифа.
- [x] `pnpm --dir apps/www build`

Dependencies: Task 3, Task 5.

Estimated scope: M.

### Task 7: Доработать раскрытия строк и экспертный режим

Описание: дать доступ к подробностям расчета без перегруза основной таблицы. В раскрытии показывать годовую сумму, cost breakdown, source refs и дополнительные editable поля.

Acceptance criteria:

- [x] Каждая строка имеет доступное раскрытие через native `details/summary` или равноценный keyboard-friendly паттерн.
- [x] В раскрытии видны годовая сумма, ФОТ, машины, материалы, подрядчики, страховые, накладные, прибыль, УСН, НДС.
- [x] Source refs видны человеку и доступны в JSON.
- [x] Экспертные поля не засоряют основной вид таблицы.

Verification:

- [x] Keyboard check: раскрытия доступны с клавиатуры.
- [x] Manual check: годовая сумма не является основной колонкой, но доступна в деталях.

Dependencies: Task 6.

Estimated scope: M.

### Task 8: Полировка минималистичного UI

Описание: привести страницу к design-code без карточной перегрузки: плотные таблицы, компактная sticky-панель, чистые секционные разделители.

Acceptance criteria:

- [x] Sticky-панель помещается в одну строку на desktop.
- [x] Mobile sticky занимает минимум высоты и не перекрывает критичные controls.
- [x] Нет многоуровневых карточек, тяжелых теней, blur и бумажного фона.
- [x] Бейджи нейтральные и фактические.
- [x] Страница читается на 320px, 768px, 1024px и 1440px.

Verification:

- [x] Manual responsive check на ключевых breakpoints.
- [x] `pnpm --dir apps/www build`

Dependencies: Task 6, Task 7.

Estimated scope: S.

### Task 9: Добавить публичные PDF-источники, если нужно

Описание: решить, должны ли посетители и агенты получать исходные PDF по публичным URL. Если да, добавить статическую публикацию PDF и ссылки из HTML/discovery.

Acceptance criteria:

- [x] Принято решение: публикуем PDF или оставляем только source refs к repo path.
- [x] Если публикуем, PDF доступны по стабильным URL под `/reglament/original/`.
- [x] JSON feed и HTML-страница ссылаются на публичные PDF URL.

Verification:

- [x] `pnpm --dir apps/www build`
- [x] Manual check: ссылки на PDF открываются из build output.

Dependencies: Task 4, Task 5.

Estimated scope: S.

### Task 10: Финальная проверка качества

Описание: закрыть точность, доступность, build и агентные поверхности перед публикацией.

Acceptance criteria:

- [x] Официальный baseline не расходится с `final.pdf`.
- [x] Все discovery routes включены в корневой API catalog.
- [x] Страница работает без JS как readable baseline.
- [x] Интерактивный JS не ломает build и не требует dev-server.
- [x] В тексте страницы нет сарказма и оценочных утверждений.

Verification:

- [x] `pnpm typecheck`
- [x] `pnpm test`
- [x] `pnpm build:main`

Dependencies: all previous tasks.

Estimated scope: S.

### Task 11: Сократить пользовательскую единицу тарифа

Описание: заменить в человекоориентированном UI единицу `₽/сотка/мес` на короткую `₽/сотка`, оставив смысл месячного тарифа в поясняющем тексте и machine-readable полях.

Acceptance criteria:

- [x] На HTML-странице и sticky-панели пользователь видит `₽/сотка`, а не `₽/сотка/мес`.
- [x] Текст страницы один раз явно объясняет, что расчетный период - месяц.
- [x] JSON feed, schema, OpenAPI и имена полей `tariff_per_sotka_month` не переименованы.
- [x] Markdown companion и llms outputs не создают двусмысленности между UI-лейблом и machine-readable полем.

Verification:

- [x] `pnpm --dir apps/www test`
- [x] `pnpm --dir apps/www build`
- [x] Manual check: sticky-панель и секционные итоги показывают короткую единицу.

Dependencies: Task 10.

Estimated scope: S.

### Task 12: Сделать источники понятными для человека

Описание: убрать из основного HTML-UI технические названия PDF вроде `final.pdf` и `cleaning.pdf`, заменив их понятными подписями разделов и документов. Публичные PDF URL и repo paths должны остаться в discovery-слое.

Acceptance criteria:

- [x] В HTML-странице источники подписаны человекочитаемо: `Итоговая смета`, `Детализация уборки`, `Детализация охраны` и т.п.
- [x] Технические имена PDF не являются основной видимой подписью ссылок в UI.
- [x] JSON feed сохраняет `pdf`, `pdf_path` и `pdf_url` для агентов.
- [x] Markdown companion и llms outputs сохраняют публичные PDF URL для проверки.

Verification:

- [x] `pnpm --dir apps/www test`
- [x] `pnpm --dir apps/www build`
- [x] Manual check: в `/reglament/` ссылки на PDF выглядят как понятные источники, а не как файловые имена.

Dependencies: Task 10.

Estimated scope: S.

### Task 13: Упростить основные controls таблицы

Описание: убрать лишний редактируемый `Годовая стоимость` из основной таблицы и сделать включение строки компактнее. Если fixed annual price нужен для экспертного сценария, перенести его в раскрытие строки.

Acceptance criteria:

- [x] В основной таблице нет отдельного input `Годовая стоимость`.
- [x] Если `fixed_price` остается редактируемым, он доступен только в раскрытии строки или экспертной зоне.
- [x] Чекбокс включения строки размещен компактно рядом с названием строки или равноценно уменьшает визуальный шум.
- [x] Подпись `Включено` не занимает отдельную строку под названием работы.

Verification:

- [x] Unit-test для controller/state helpers, если меняется mapping editable fields.
- [x] `pnpm --dir apps/www test`
- [x] `pnpm --dir apps/www build`
- [x] Manual check: отключение строки и изменение fixed price, если он сохранен, продолжают пересчитывать тариф.

Dependencies: Task 10.

Estimated scope: M.

### Task 14: Исправить шаги числовых inputs

Описание: для целочисленных значений вроде дней, раз в год, количества постов и светильников использовать `step=1`, чтобы стрелки number-input не меняли сотые доли.

Acceptance criteria:

- [x] Частоты и объемы с целочисленным baseline получают `step=1`, если в данных явно не задан другой шаг.
- [x] Дробные значения вроде `0.2`, `6201.6`, ставок и коэффициентов сохраняют дробный шаг.
- [x] Данные не требуют ручного `step: 1` для каждой целочисленной строки, если это можно вывести из baseline.

Verification:

- [x] Unit-test проверяет `step=1` для целочисленной частоты и дробный step для дробного значения.
- [x] `pnpm --dir apps/www test`
- [x] `pnpm --dir apps/www build`

Dependencies: Task 10.

Estimated scope: S.

### Task 15: Типографировать денежные и тарифные значения

Описание: привести форматирование стоимостей к единому helper-у с неразрывными пробелами между разрядами, числом, знаком рубля и единицами измерения там, где значения рендерятся в HTML или текстовые companion-поверхности.

Acceptance criteria:

- [x] HTML-страница использует общий formatter для `₽`, `₽/год`, `₽/сотка` и дельт.
- [x] Client controller использует тот же формат или совместимый helper, чтобы hydrated значения не меняли типографику.
- [x] Markdown companion и llms outputs не получают сломанную разметку и остаются читаемыми plain text.
- [x] В тестах видны неразрывные пробелы через helper или snapshot-представление.

Verification:

- [x] Unit-test formatter-а или controller-state format output.
- [x] `pnpm --dir apps/www test`
- [x] `pnpm --dir apps/www build`

Dependencies: Task 11.

Estimated scope: M.

### Task 16: Перепроверить спорную строку благоустройства

Описание: сверить `improvement-road-surface-repair` с `final.pdf` и `improvement.pdf`: сейчас строка final называется ремонтом покрытия дорог/площадок, а ближайшая крупная строка детализации похожа на замену элементов периметрального ограждения.

Acceptance criteria:

- [x] Принято и зафиксировано решение: строка сопоставлена корректно или данные исправлены.
- [x] Если данные исправлены, source refs, title, tags, note и computed baseline остаются согласованными.
- [x] Если данных недостаточно, caveat сохранен явно и без оценочных формулировок.

Verification:

- [x] Unit-test baseline-итогов продолжает сходиться с `final.pdf`.
- [x] `pnpm --dir apps/www test`
- [x] Manual check: source refs строки не вводят пользователя в заблуждение.

Dependencies: Task 10.

Estimated scope: S.

### Task 17: Оценить поле площади участка

Описание: вернуться к открытому вопросу `мой участок` и решить, нужно ли добавлять расчет платежа для конкретной площади участка после стабилизации базового калькулятора.

Решение: поле площади участка остается вне текущего продукта. Первый релиз сохраняет фокус на общей проверяемой единице `₽/сотка`, а к расчету частного платежа для конкретного участка можно вернуться отдельной future-задачей, если появится подтвержденная пользовательская потребность.

Acceptance criteria:

- [x] Принято решение: оставить вне продукта, добавить в UI или записать отдельный future task.
- [x] Поле не добавляется, поэтому основной фокус страницы остается на `₽/сотка`, а не на частном платеже пользователя.
- [x] Если поле не добавляется, открытый вопрос в спецификации обновлен.

Verification:

- [x] Если меняется только спецификация: review diff.
- [x] UI/logic не менялись; `pnpm --dir apps/www test` и `pnpm --dir apps/www build` для этой ветки решения не требуются.

Dependencies: Task 11.

Estimated scope: S.

### Task 18: Исправить раскрытие деталей строки в таблице

Описание: разобраться с layout раскрытия `details/summary`, которое сейчас визуально раскрывает подробности как будто внутри одной колонки `Работа`, хотя содержание относится ко всей строке таблицы.

Acceptance criteria:

- [x] Детали строки визуально читаются как раскрытие всей строки, а не только первой колонки.
- [x] Решение остается keyboard-friendly и сохраняет native `details/summary` или равноценную доступность.
- [x] Desktop-таблица не получает лишнюю вложенную карточность и сохраняет плотный табличный вид.
- [x] Mobile horizontal scroll и раскрытия не ломают чтение подробностей.

Verification:

- [x] `pnpm --dir apps/www build`
- [x] Manual check: открыть детали первой строки на desktop и убедиться, что блок деталей не выглядит ограниченным одной колонкой.
- [x] Manual responsive check: раскрытия читаются на 320px и 768px.

Dependencies: Task 10.

Estimated scope: M.

### Task 19: Отсортировать секции по вкладу в тариф

Описание: изменить исходный порядок секций на странице так, чтобы при первом открытии они шли по убыванию официального вклада в тариф. Автоматически пересортировывать секции после пользовательских изменений не нужно.

Acceptance criteria:

- [x] HTML-страница изначально показывает секции по убыванию `official.tariff_per_sotka_month`.
- [x] Порядок секций не меняется при редактировании полей калькулятора.
- [x] JSON feed сохраняет исходную структуру данных без требования сортировать machine-readable payload для UI.
- [x] Anchor/id секций и source refs остаются стабильными.

Verification:

- [x] Unit-test или route smoke проверяет порядок секций в HTML/view-model, если есть удобная тестируемая точка.
- [x] `pnpm --dir apps/www test`
- [x] `pnpm --dir apps/www build`
- [x] Manual check: первым разделом видна самая крупная секция по тарифу, а после изменения поля порядок не прыгает.

Dependencies: Task 10.

Estimated scope: S.

### Task 20: Убрать лишние разделители секционных таблиц

Описание: сделать переходы между секционными таблицами визуально спокойнее: без дополнительных декоративных разделителей, которые дробят страницу сильнее, чем сами таблицы.

Acceptance criteria:

- [x] Между секционными таблицами нет лишних горизонтальных разделителей сверх естественных отступов и границ таблиц.
- [x] Секции остаются читаемыми как отдельные блоки с заголовком и итогом.
- [x] Страница не получает новую карточную вложенность или тяжелые визуальные контейнеры.

Verification:

- [x] `pnpm --dir apps/www build`
- [x] Manual check: секции читаются как единый список таблиц без визуальной «лесенки» разделителей.

Dependencies: Task 10.

Estimated scope: S.

### Task 21: Сделать чекбокс строки инлайновым

Описание: перестроить чекбокс включения позиции в названии работы так, чтобы он был инлайновым элементом текста и при переносе длинного названия следующая строка не получала лишний отступ из-за чекбокса.

Решение: задачу не реализуем. Текущий компактный чекбокс рядом с названием строки оставляем без изменений, чтобы не менять уже проверенный табличный UI ради малозаметной правки переносов.

Acceptance criteria:

- [x] Принято решение не менять layout чекбокса строки в текущем релизе.
- [x] Текущий чекбокс остается компактно рядом с названием строки.
- [x] Управление сохраняет существующий `aria-label` и keyboard-доступность без изменений кода.

Verification:

- [x] Spec-only decision: review diff.
- [x] UI/logic не менялись; `pnpm --dir apps/www build`, responsive и keyboard checks для этой ветки решения не требуются.

Dependencies: Task 13.

Estimated scope: S.

### Task 22: Упростить sticky-итог до текущего тарифа

Описание: в sticky-строке оставить только динамически пересчитываемое значение текущего тарифа и разместить его справа. Не показывать одновременно официальный тариф, текущий тариф и дельту.

Acceptance criteria:

- [ ] Sticky-панель показывает одну основную цифру текущего тарифа.
- [ ] Значение выровнено справа и обновляется при изменении inputs.
- [ ] Официальный baseline и дельта остаются доступны в основном контенте или данных, но не перегружают sticky-строку.
- [ ] Кнопка сброса остается доступной и не спорит с главным числом.

Verification:

- [ ] `pnpm --dir apps/www build`
- [ ] Manual check: изменить поле калькулятора и увидеть обновление единственного sticky-тарифа справа.
- [ ] Manual responsive check: sticky-строка остается компактной на mobile.

Dependencies: Task 6, Task 11.

Estimated scope: S.

### Task 23: Убрать колонку изменения из таблицы

Описание: убрать отдельную колонку `Изменение`; вместо нее показывать пересчитанный вклад в колонке `Вклад в тариф` и подсвечивать значение зеленым или красным, если оно отличается от официального baseline.

Acceptance criteria:

- [ ] В секционных таблицах нет отдельной колонки `Изменение`.
- [ ] Колонка `Вклад в тариф` показывает текущее пересчитанное значение строки.
- [ ] Если значение отличается от baseline, оно получает нейтральную green/red-подсветку по направлению изменения.
- [ ] Информация о baseline или delta не пропадает из machine-readable данных.

Verification:

- [ ] Unit-test для controller/state helpers, если меняется DOM mapping вклада и дельты.
- [ ] `pnpm --dir apps/www test`
- [ ] `pnpm --dir apps/www build`
- [ ] Manual check: изменить строку и увидеть цветовое изменение значения во `Вклад в тариф` без отдельной колонки `Изменение`.

Dependencies: Task 6, Task 13.

Estimated scope: M.

## Checkpoints

Checkpoint after Tasks 1-3:

- [x] Данные и расчет сходятся с официальной сметой.
- [x] Есть тесты на baseline и основные формулы.

Checkpoint after Tasks 4-6:

- [x] Раздел доступен людям и агентам.
- [x] Базовая интерактивность работает end-to-end.

Checkpoint after Tasks 7-10:

- [x] UI не перегружен деталями.
- [x] Discovery полный.
- [x] Build, tests и typecheck проходят.

Checkpoint after follow-up Tasks 11-23:

- [ ] UI-лейблы, источники и controls соответствуют follow-up notes.
- [ ] Спорная строка благоустройства перепроверена или явно помечена caveat.
- [ ] Раскрытия строк читаются как детали всей строки, а не одной колонки.
- [ ] Секции стартово отсортированы по официальному вкладу в тариф без runtime-пересортировки.
- [ ] Sticky-итог, чекбоксы строк, разделители таблиц и колонка вклада соответствуют новым UI follow-up notes.
- [ ] Build и tests проходят после всех follow-up правок.
