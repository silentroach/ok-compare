# Progress

## Current Step

Migration complete.

## Active Wave

- Runtime task: `task-1779356478-501f`.
- Runtime task key: `code-assist:raw-to-domain-data:step-13:final-guardrails`.
- Artifact: `docs/migrations/raw-to-domain-data/tasks/13-final-guardrails.md`.
- Status: Step 13 closed; migration complete.

## Verification Notes

- Step 13 RED: `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts` failed because the new guard found the remaining compare `src/compare/lib/schema.ts` exported Zod-inferred domain shortcut types and documented the existing allowlist for raw/public legacy boundaries.
- Step 13 GREEN: added `src/lib/raw-domain-boundary.architecture.test.ts`, removed temporary compare schema domain shortcut exports, changed the compare collection type contract test to assert raw collection data against `RawSettlement`, and added the future-agent Raw DTO -> domain model -> Public DTO rule to `apps/www/AGENTS.md`.
- Step 13 focused verification passed: `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts src/compare/lib/schema.test.ts`.
- Step 13 final verification passed: `pnpm exec prettier --check apps/www/src/lib/raw-domain-boundary.architecture.test.ts apps/www/src/compare/lib/schema.ts apps/www/src/compare/lib/schema.test.ts apps/www/AGENTS.md docs/migrations/raw-to-domain-data/CONTEXT.md docs/migrations/raw-to-domain-data/progress.md`, `pnpm --filter @shelkovo/www test`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm build`.
- Step 13 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts` failed after removing the broad raw-token allowlist, exposing internal status mode values `active_incident`/`no_incidents` plus other legacy raw-like tokens that now need path-scoped justification instead of global allowance.
- Step 13 rejection fix GREEN: converted internal status quiet-days mode values to domain `activeIncident`/`noIncidents`, mapped them back to legacy public `active_incident`/`no_incidents` in `status/public-dto.ts`, migrated compare sitemap source metadata from `date_checked` to `dateChecked`, and replaced the broad guard allowlist with file-scoped exceptions carrying justification.
- Step 13 rejection fix verification passed: `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts src/lib/status/load.test.ts src/lib/status/schema.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www test`, `pnpm --filter @shelkovo/www typecheck`, `pnpm build`, final `pnpm exec prettier --check ...`, and post-format focused tests plus typecheck.
- Step 13 schema-boundary rejection fix RED: `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts` failed on a new guard proving `news/schema.ts`, `status/schema.ts`, and `people/schema.ts` still imported or re-exported domain types from `./types`.
- Step 13 schema-boundary rejection fix GREEN: removed the temporary domain type compatibility re-exports from the news/status/people schema modules, moved `EMPTY_PERSON_BACKLINKS` to the people domain types module, migrated internal consumers to import domain interfaces from `types.ts` directly, and kept schema imports for section constants/raw helper types only.
- Step 13 schema-boundary rejection fix verification passed: `pnpm --filter @shelkovo/www test -- src/lib/raw-domain-boundary.architecture.test.ts src/lib/news src/lib/status src/lib/people src/components/news src/components/status`, `pnpm --filter @shelkovo/www typecheck`, Step 13 prettier check for changed files, `pnpm build`, and full `pnpm --filter @shelkovo/www test`.
- Step 10 RED: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts src/lib/people/schema.test.ts` failed because `src/lib/people/mapper.ts` and `src/lib/people/raw-schema.ts` did not exist and people dataset/profile/backlink internals still exposed raw fields such as `by_slug`, `mention_registry`, `name_cases`, `markdown_url`, `source_id`, and `mentioned_at`.
- Step 10 GREEN: added `RawPersonProfileSchema`, handwritten readonly people domain types, `mapRawPersonProfile`, `mapRawPersonContact`, and `mapRawPersonMentionTarget`; migrated people dataset/profile/backlink internals to `bySlug`, `mentionRegistry`, `nameCases`, `markdownUrl`, `sourceId`, `htmlUrl`, `mentionedAt`, and `sortKey`, while keeping public people discovery/llms JSON names as DTO boundaries.
- Step 10 verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts src/lib/people/schema.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`.
- Planner advanced to Step 11 after Step 10 finalization; created runtime task `task-1779354074-8c97` for `docs/migrations/raw-to-domain-data/tasks/11-people-public-and-backlinks.md`.
- Step 11 RED: `pnpm --filter @shelkovo/www test -- src/lib/people/public-dto.test.ts` failed because `src/lib/people/public-dto.ts` did not exist; the new test documents the preserved legacy public `name_cases`/`html_url`/`markdown_url`/`source_id`/`mentioned_at` people JSON contract and requires a graph-to-backlinks adapter.
- Step 11 GREEN: added `src/lib/people/public-dto.ts`, moved people JSON payload DTO types/builders out of `discovery.ts`, routed `buildPeoplePayload` through `buildPeoplePublicPayload`, and moved domain mention graph -> `PersonBacklinks` conversion behind `createPeopleBacklinksFromGraph` instead of keeping it inline in the loader.
- Step 11 code-simplification pass: kept the migration narrow; removed the stale `absoluteUrl` import from `discovery.ts` after the adapter move and changed the `llms.test.ts` fixture to camelCase domain backlink fields.
- Step 11 verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and a built `/people/data/people.json` harness confirming 3 profiles, legacy public snake_case keys, and no camelCase public JSON leaks.
- Step 11 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts` failed because `load.ts` imported `createPeopleBacklinksFromGraph` from `./public-dto`, making the internal loader depend on the public DTO module.
- Step 11 rejection fix GREEN: moved the graph-to-domain backlinks adapter to internal `src/lib/people/backlinks.ts`, changed `load.ts` to import from `./backlinks`, kept `public-dto.ts` limited to domain -> legacy public people payload conversion, and added a guard so `load.ts` cannot import `./public-dto`.
- Step 11 rejection fix verification passed: `pnpm --filter @shelkovo/www test -- src/lib/people/load.test.ts src/lib/people/public-dto.test.ts src/lib/people/backlinks.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, `pnpm exec prettier --check apps/www/src/lib/people/backlinks.ts apps/www/src/lib/people/backlinks.test.ts apps/www/src/lib/people/load.ts apps/www/src/lib/people/load.test.ts apps/www/src/lib/people/public-dto.ts apps/www/src/lib/people/public-dto.test.ts docs/migrations/raw-to-domain-data/progress.md .ralph/agent/scratchpad.md`, and a built `/people/data/people.json` harness confirming 3 profiles, legacy `html_url`/`markdown_url`/`name_cases`/`source_id`, populated backlinks, and no camelCase public JSON leaks. Recorded expected RED failure memory `mem-1779355020-3d8a` and formatting fix memory `mem-1779355074-689d`.
- Step 11 finalizer verification passed: re-read migration docs, ADR-013/ADR-011, app rules and Task 11; confirmed `load.ts` imports the graph-to-domain backlink adapter from internal `./backlinks` while `public-dto.ts` remains a legacy public payload adapter. `pnpm --filter @shelkovo/www test -- src/lib/people`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and Step 11 prettier check passed. Built people JSON/Markdown harness confirmed 3 profiles, 3 backlinks, legacy public `html_url`/`markdown_url`/`name_cases`/`source_id`, no camelCase public JSON leaks, profile contacts, and rendered backlink Markdown. Recorded harness assertion memory `mem-1779355535-b39c`.
- Planner advanced to Step 12 after Step 11 finalization; created runtime task `task-1779355664-cde2` for `docs/migrations/raw-to-domain-data/tasks/12-public-dto-contracts.md`.
- Step 12 RED: `pnpm --filter @shelkovo/www test -- src/lib/root-discovery-routes.test.ts` failed because the new public DTO boundary guard found `docs/migrations/raw-to-domain-data/inventory.md` missing the required `Intentional legacy public fields` inventory section.
- Step 12 GREEN: added the inventory section listing intentional legacy public fields for compare, news, status, people, and public Markdown companion frontmatter; added a root discovery guard proving compare/news/status/people public DTO or serializer boundaries exist and the legacy field inventory covers representative published `snake_case` keys.
- Step 12 code-simplification pass: kept the change narrow; no public JSON shape, schema, OpenAPI, llms, or route implementation changes were needed because existing section adapters and discovery schemas already describe the current legacy public DTOs.
- Step 12 verification passed: `pnpm --filter @shelkovo/www test -- src/lib/root-discovery-routes.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/root-discovery-routes.test.ts src/lib/news src/lib/status src/lib/people src/compare/lib`, `pnpm --filter @shelkovo/www typecheck`, `pnpm exec prettier --check apps/www/src/lib/root-discovery-routes.test.ts docs/migrations/raw-to-domain-data/inventory.md docs/migrations/raw-to-domain-data/progress.md .ralph/agent/scratchpad.md`, and `pnpm --filter @shelkovo/www build`. Recorded expected RED failure memory `mem-1779355860-e99a`.
- Planner advanced to Step 9 after Step 8 finalization; created runtime task `task-1779351581-a498` for `docs/migrations/raw-to-domain-data/tasks/09-status-public-ui-migration.md`.
- Step 9 RED: `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts src/components/status/StatusServiceTimeline.test.ts` failed because the status timeline browser DTO still serialized grouped tooltip items as raw-style `is_active`, `started_iso`, `started_has_time`, `ended_iso`, `ended_has_time`, and `total_minutes`.
- Step 9 GREEN: added a typed camelCase timeline tooltip DTO adapter in `src/lib/status/timeline-tooltip-dto.ts`, migrated `StatusServiceTimeline.astro` serialization and `timeline.dom.ts` parsing to camelCase DTO keys, and added a guard preventing raw status field names in status UI/browser DTO contracts.
- Step 9 public adapter pass: moved the legacy status JSON payload adapter to explicit `src/lib/status/public-dto.ts`, routed `/status/data/status.json` through `buildStatusPublicPayload`, and kept discovery schema/catalog helpers separate from payload construction while preserving the existing public snake_case contract.
- Step 9 verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`. Built contract harness confirmed `/status/data/status.json` has 18 incidents with legacy `started_at`/`is_active`/`source_url`, no camelCase public JSON leaks, and built status HTML uses camelCase tooltip DTO keys without raw tooltip keys.
- Step 9 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts` failed because `StatusIncidentMeta.astro` still used raw status UI fields `source_url` and `applies_to_all_areas`.
- Step 9 rejection fix GREEN: extended the status UI/browser DTO guard to scan `StatusIncidentMeta.astro` for `source_url` and `applies_to_all_areas`, then migrated the component to domain `sourceUrl` and `appliesToAllAreas` fields.
- Step 9 rejection fix verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, `pnpm --filter @shelkovo/www build`, and built status HTML grep confirmed no `source_url`/`applies_to_all_areas` leak in status HTML.
- Step 9 finalizer verification passed: re-read migration docs, ADR-013/ADR-011, app rules and Task 09; `pnpm --filter @shelkovo/www test -- src/lib/status src/components/status`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build` passed. Built `/status/data/status.json` has 18 incidents, keeps legacy `started_at`/`started_has_time`/`is_active`/`source_url`, has no camelCase public JSON leaks, and `/status/index.html` uses camelCase tooltip DTO keys without raw status UI token leaks.
- Planner advanced to Step 10 after Step 9 finalization; created runtime task `task-1779352900-4579` for `docs/migrations/raw-to-domain-data/tasks/10-people-raw-domain-foundation.md`.
- Step 8 RED: `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts` failed because `src/lib/status/mapper.ts` and `src/lib/status/raw-schema.ts` did not exist and the status dataset still exposed raw-style `by_id`/`started_iso` domain fields.
- Step 8 GREEN: added reusable `RawStatusIncidentSchema`, handwritten readonly status domain types in `types.ts`, and explicit raw-to-domain mappers for status service/kind/area values and incident fields. Migrated status loader/domain call sites to camelCase while keeping timeline DOM tooltip payload and public discovery output as DTO boundaries.
- Step 8 verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts` and `pnpm --filter @shelkovo/www typecheck`.
- Step 8 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts` failed because `src/lib/sitemap.ts` and `src/lib/sitemap-data.ts` still exposed status sitemap source contracts as `status_incidents`, `started_iso`, `ended_iso`, and `has_page`.
- Step 8 rejection fix GREEN: extended the status raw/domain guard to cover status sitemap source contracts, then migrated only the status sitemap input/source fields to `statusIncidents`, `startedIso`, `endedIso`, and `hasPage`. Existing public status JSON/timeline DTO snake_case boundaries were left untouched.
- Step 8 rejection fix verification passed: `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`.
- Step 8 finalizer verification passed: re-read migration docs, ADR-013/ADR-011, app rules and Task 08; `pnpm --filter @shelkovo/www test -- src/lib/status/load.test.ts src/lib/status/schema.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/status/schema.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build` passed. Built `/status/data/status.json` has 18 incidents, keeps legacy public `started_at`/`is_active`/`source_url`, has no camelCase status domain key leaks, and built sitemap contains status URLs without raw status sitemap tokens.
- Step 7 RED: `pnpm --filter @shelkovo/www test -- src/lib/news/index.architecture.test.ts src/lib/news/load.test.ts` failed because news article/event/list/domain contracts still exposed raw `snake_case` fields such as `markdown_url`, `published_at`, `starts_iso`, `ics_url`, `by_id`, and `by_tag`.
- Step 7 GREEN: migrated `NewsArticle`, `NewsListArticle`, `NewsEvent`, archive/tag/home/dataset structures and news UI/rendering call sites to camelCase domain fields. Raw frontmatter field reads remain at the loader boundary, and legacy `articles.json`/discovery shape is now built via explicit public DTO adapters in `src/lib/news/public-dto.ts`.
- Step 7 verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`.
- Step 7 built JSON check passed: `/news/data/articles.json` still has 14 articles and keeps legacy public `html_url`, `markdown_url`, and `source_url` keys while domain code uses camelCase.
- Step 7 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/lib/news/index.architecture.test.ts` failed because `src/lib/sitemap.ts` still exposed news sitemap contracts as `news_articles`, `published_iso`, and `updated_iso`.
- Step 7 rejection fix GREEN: extended the news raw/domain guard to cover `src/lib/sitemap.ts` and `src/lib/sitemap-data.ts`, then migrated only the news sitemap source contract to `newsArticles`, `publishedIso`, and `updatedIso` while leaving status/compare sitemap fields for their own migration steps.
- Step 7 rejection fix verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news/index.architecture.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`.
- Step 7 finalizer verification passed: re-read migration docs, ADR-013/ADR-011, app/news rules and Task 07; `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www test -- src/lib/news/index.architecture.test.ts src/lib/sitemap.test.ts`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build` passed. Built `/news/data/articles.json` has 14 articles, 4 events, legacy public `published_at`/`html_url`/`markdown_url`, absolute `events[].ics_url`, and no camelCase domain key leaks.
- Step 6 RED: `pnpm --filter @shelkovo/www test -- src/lib/news/mapper.test.ts src/lib/news/index.architecture.test.ts` failed because `mapper.ts`, `types.ts` and `raw-schema.ts` did not exist and news schemas/domain interfaces still lived inline/in `schema.ts`.
- Step 6 GREEN: added reusable `RawNewsAuthorSchema` and `createRawNewsArticleSchema`, moved news domain interfaces into `types.ts`, introduced `mapRawNewsAuthor`, and migrated author domain field `short_name` to `shortName` without changing article public output.
- Step 6 verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`.
- Step 6 typecheck passed: `pnpm --filter @shelkovo/www typecheck`.
- Step 6 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/lib/news/index.architecture.test.ts` failed because `NewsMeta.astro` still exposed `author.short_name` in its component prop contract.
- Step 6 rejection fix GREEN: added a news UI contract guard for raw author field names, migrated `NewsMeta.astro` to `author.shortName`, and confirmed no `short_name` tokens remain in news components/pages.
- Step 6 rejection fix verification passed: `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build`.
- Step 6 finalizer verification passed: re-read migration docs, ADR-013/ADR-011, app/news rules and Task 06; `pnpm --filter @shelkovo/www test -- src/lib/news`, `pnpm --filter @shelkovo/www typecheck`, and `pnpm --filter @shelkovo/www build` passed. Built news JSON has 14 articles, no `short_name` leak under `/news`, and the rendered seeded article shows the mapped author label.
- Step 5 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/compare/lib/index.architecture.test.ts` failed because `full.ts` still exposed domain `ComparisonResult`/`Stats` in public payload types and JSON routes assembled payload bodies inline.
- Step 5 rejection fix GREEN: added explicit public stats/comparison DTO adapters in `public-dto.ts`, added whole-payload adapters `toFullPayload`/`toExplorerPayload`, and routed both public JSON endpoints through those adapters without changing the built JSON shape.
- Step 5 rejection fix focused tests passed: `pnpm --filter @shelkovo/www test -- src/compare/lib/index.architecture.test.ts src/compare/lib/full.test.ts src/compare/lib/explorer.test.ts`.
- Step 5 rejection fix compare suite passed: `pnpm --filter @shelkovo/www test -- src/compare`.
- Step 5 rejection fix typecheck passed: `pnpm --filter @shelkovo/www typecheck`.
- Step 5 rejection fix build passed: `pnpm --filter @shelkovo/www build`; built `settlements.json` and `explorer.json` still expose top-level `settlements,stats,comparisons`, 13 stats fields, and 37 comparisons.
- Step 5 RED: `pnpm --filter @shelkovo/www test -- src/compare/lib/index.architecture.test.ts` failed on the new guard because compare Astro/Svelte UI still referenced raw `snake_case` settlement fields.
- Step 5 GREEN: compare routes and Svelte components now consume domain/component `camelCase` fields; `data/explorer.json` is a lightweight camelCase DTO from `toExplorer`; the full `data/settlements.json` legacy contract remains behind `toFull` as the intentional public adapter.
- Step 5 focused verification passed: `pnpm --filter @shelkovo/www test -- src/compare/lib/index.architecture.test.ts src/compare/lib/explorer.test.ts src/compare/components`.
- Step 5 compare suite passed: `pnpm --filter @shelkovo/www test -- src/compare`.
- Step 5 Svelte autofixer reported no issues for changed Svelte files; `SettlementsExplorer.svelte` kept pre-existing suggestions around effect-managed fetch state and a local `Map`.
- Step 5 typecheck passed: `pnpm --filter @shelkovo/www typecheck`.
- Step 4 RED: `pnpm --filter @shelkovo/www test -- src/compare/lib/data.test.ts` failed because `loadSettlements()` returned raw collection `entry.data` with `snake_case` fields instead of a domain `Settlement`.
- Step 4 GREEN: `loadSettlements()` now maps collection data through `mapRawSettlement`; compare core modules use domain `Settlement` fields, domain tariff units and `settlement/lots.ts` domain lot helpers. Legacy JSON-facing shapes remain in `explorer.ts` and `full.ts` public adapters.
- Step 4 tests passed: `pnpm --filter @shelkovo/www test -- src/compare/lib`.
- Step 4 typecheck passed: `pnpm --filter @shelkovo/www typecheck`.
- Step 3 builder pass added compare settlement raw/domain foundation files under `apps/www/src/compare/lib/settlement`: `schema.ts` exposes `Raw*Schema` contracts and `z.output` raw types, `types.ts` defines handwritten readonly camelCase domain interfaces, and `mapper.ts` maps `RawSettlement` to domain `Settlement` with explicit enum/string translations for `rub_per_sotka`, `rub_per_lot`, `rub_fixed`, `partial_asphalt`, and `checkpoint_only`.
- Step 3 RED: `pnpm --filter @shelkovo/www test -- src/compare/lib/settlement/mapper.test.ts` failed as expected before implementation because `./mapper` did not exist.
- Step 3 GREEN verification passed: `pnpm --filter @shelkovo/www test -- src/compare/lib/schema.test.ts src/compare/lib/settlement/mapper.test.ts`.
- Step 3 typecheck passed: `pnpm --filter @shelkovo/www typecheck`.
- Step 3 formatting passed after write: `pnpm exec prettier --check "apps/www/src/compare/lib/settlement/**/*.ts" "apps/www/src/compare/lib/schema.test.ts" "docs/migrations/raw-to-domain-data/progress.md" ".ralph/agent/scratchpad.md"`.
- Step 3 rejection fix RED: `pnpm --filter @shelkovo/www test -- src/compare/lib/settlement/mapper.test.ts` failed because unsupported raw tariff units still fell through to `fixed`.
- Step 3 rejection fix GREEN: `mapTariffUnit` now has an explicit `rub_fixed -> fixed` branch and throws on unsupported raw tariff units. `pnpm --filter @shelkovo/www test -- src/compare/lib/schema.test.ts src/compare/lib/settlement/mapper.test.ts` passed.
- Step 3 rejection fix typecheck passed: `pnpm --filter @shelkovo/www typecheck`.
- Step 3 rejection fix formatting passed: `pnpm exec prettier --check "apps/www/src/compare/lib/settlement/**/*.ts" "apps/www/src/compare/lib/schema.test.ts" "docs/migrations/raw-to-domain-data/progress.md" ".ralph/agent/scratchpad.md"`.
- Step 1 finalizer verification passed: `inventory.md` satisfies Task 01 acceptance criteria and `pnpm exec prettier --check "docs/migrations/raw-to-domain-data/**/*.md"` passed.
- Step 2 builder pass updated generic mention contracts to internal `camelCase` fields, adjusted news/status/people source adapters, kept people discovery/backlink legacy names in the public-facing adapter path, and updated focused tests.
- Step 2 RED: `pnpm --filter @shelkovo/www test -- src/lib/mentions src/lib/news/mentions.test.ts src/lib/status/mentions.test.ts src/lib/people/mentions.test.ts` failed as expected on old `snake_case` mention contracts.
- Step 2 GREEN verification passed: `pnpm --filter @shelkovo/www test -- src/lib/mentions src/lib/news/mentions.test.ts src/lib/status/mentions.test.ts src/lib/people/mentions.test.ts src/lib/people/discovery.test.ts`.
- Step 2 typecheck passed: `pnpm --filter @shelkovo/www typecheck`.
- Step 2 finalizer verification passed: generic mention contracts and news/status/people adapters use internal `camelCase`; broader mention and people regression tests plus `@shelkovo/www` typecheck passed.

## Completed Steps

- Step 1 - Boundary inventory: runtime task `task-1779307015-11ea` closed; created `docs/migrations/raw-to-domain-data/inventory.md`.
- Step 2 - Mention internal contracts: runtime task `task-1779307543-6e6f` closed; migrated shared mention contracts and adapter boundaries.
- Step 3 - Compare raw/domain foundation: runtime task `task-1779308230-b77a` closed; added compare settlement raw schemas, readonly domain interfaces, mapper, and focused mapper/schema tests.
- Step 4 - Compare core domain migration: runtime task `task-1779309212-46e0` closed; completed compare loaders and core calculations migration to domain `Settlement`; focused compare lib tests and `@shelkovo/www` typecheck passed.
- Step 5 - Compare UI and public adapters: runtime task `task-1779345053-771f` closed; migrated compare routes/Svelte components to domain/component camelCase DTOs, added explicit public compare JSON DTO adapters, and verified compare tests, typecheck, build, and built JSON contracts.
- Step 6 - News raw/domain foundation: runtime task `task-1779346730-0c77` closed; added reusable news raw schemas, readonly domain types, explicit author mapper, `NewsAuthor.shortName`, and a guard preventing raw author `short_name` in news UI contracts.
- Step 7 - News article domain migration: runtime task `task-1779348032-8b0c` closed; migrated news article, event, archive/list, dataset, and news sitemap internals to camelCase domain contracts while keeping legacy public article JSON behind explicit public DTO adapters.
- Step 8 - Status raw/domain foundation: runtime task `task-1779349886-8a97` closed; added reusable status raw schema, readonly domain types, explicit status mappers, camelCase loader/sitemap internals, and preserved legacy public status DTO boundaries.
- Step 9 - Status UI and public adapters: runtime task `task-1779351581-a498` closed; moved timeline browser payloads and status UI internals to camelCase domain/component DTO contracts, added explicit status public DTO adapter for legacy JSON, and guarded status UI raw-token regressions.
- Step 10 - People raw/domain foundation: runtime task `task-1779352900-4579` closed; added people raw schema, readonly domain types, raw-to-domain mapper/contact normalization, camelCase people loader/registry/view/profile internals, and people architecture guards.
- Step 11 - People public surfaces and backlinks: runtime task `task-1779354074-8c97` closed; moved people public payload construction to explicit legacy public DTO adapters and kept graph-to-domain backlinks in an internal people module.
- Step 12 - Public DTO contracts across sections: runtime task `task-1779355664-cde2` closed; explicit public DTO/serializer boundaries and intentional legacy public fields inventory cover compare, news, status and people public contracts.
- Step 13 - Final guardrails and cleanup: runtime task `task-1779356478-501f` closed; final architecture guardrails, compatibility cleanup, completed migration context and future-agent rules are in place.
